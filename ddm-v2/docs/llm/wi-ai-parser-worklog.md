# WI AI Parser 交付追蹤（Worklog）

**文件類型：** 交付追蹤紀錄（單一追蹤入口；不定義規格）
**規格：** [wi-ai-parser-implementation-spec.md](wi-ai-parser-implementation-spec.md)（§20 定義本文件的填寫規則）
**建立日期：** 2026-08-07

> 填寫規則摘要（完整版見 spec §20）：
> - Phase 狀態、checklist 勾選、checkpoint 結果 → 本文件。
> - 卡點（規格沒寫／規格與現實衝突）→ Blocker Log，**先記再解**；不刪列、不改寫歷史。
> - 決策分級：D1 架構級 → 開 ADR；D2 規格級 → 修 spec＋升版本；D3 實作級 → 本文件 Decision Log。
> - 不確定分級時從嚴（往上一級記）。

---

## 1. Phase 狀態總覽

| Phase | 內容 | 狀態 | 開工 | 完成 | Checkpoint |
|-------|------|------|------|------|------------|
| L0 | 契約 + AI 表 migration + rule parser 包進新契約 + run 落庫 | `completed` | 2026-08-07 | 2026-08-07 | code-review APPROVE_WITH_NITS |
| L1 | LLM planner adapter（structured output、cache、fallback） | `completed` | 2026-08-07 | 2026-08-07 | code-review APPROVE |
| L2 | Slot linking + deterministic compiler + engine gate | `completed` | 2026-08-07 | 2026-08-07 | code-review APPROVE_WITH_NITS |
| L3 | 審核 UI + review events + feedback candidates | `completed` | 2026-08-07 | 2026-08-10 | impl checkpoint；§0.1 現場 demo 待人 |
| L4 | 批次 parse job（**需 ADR-026/027 accepted**） | `completed` | 2026-08-10 | 2026-08-10 | code-review APPROVE |
| R1 | Worksheet revision 樂觀鎖（ADR-027 §2） | `completed` | 2026-08-11 | 2026-08-11 | code-review APPROVE_WITH_NITS |
| R2a | Policy manifests（modeling／Level）+ worksheet FK（ADR-027 §3） | `completed` | 2026-08-11 | 2026-08-11 | code-review APPROVE_WITH_NITS |
| R2b | Level validation runs + publish gate（ADR-027 §9） | `completed` | 2026-08-11 | 2026-08-11 | code-review APPROVE_WITH_NITS |
| R3b | Transactional outbox（review／save 同交易）（ADR-027 §5） | `completed` | 2026-08-11 | 2026-08-11 | code-review APPROVE_WITH_NITS |
| R3a | Method context `wi_row_contexts`（ADR-027 §4） | `completed` | 2026-08-11 | 2026-08-11 | code-review APPROVE_WITH_NITS |

狀態值：`not_started / in_progress / blocked / completed / blocked_on_adr`

## 2. Checklist 進度（對照 spec §18）

### L0
- [x] L0-1 `contracts.py` + 單元測試
- [x] L0-2 `normalize_with_map` + `quantities.py` + 測試
- [x] L0-3 `models/v2/ai_ops.py` + Alembic migration（AI-1 / v2_0026）+ bundle seed；`alembic upgrade head` 已套用
- [x] L0-4 `rule_plan_adapter.py` + `wi_ai_service.parse()`（rule 路徑）+ run 落庫 + 冪等
- [x] L0-5 `nl_draft.py` 加法擴充 + integration 相容／冪等測試（9 passed）
- [x] L0 checkpoint（code review；資安依 user 指示延後）
  - 驗證：unit + `test_nlp_api` 綠；migration v2_0026/v2_0027；review [L0 WI AI code review](b4377189-48be-4e40-9f68-42988e7b3bb4) → REQUEST_CHANGES 後修畢 → APPROVE_WITH_NITS。
  - 資安完整席位：延後（功能優先）。

### L1
- [x] L1-1 `llm_client.py` + settings 欄位 + `prompts/plan_v1.py`
- [x] L1-2 `llm_planner.py` + `validate_planner_output` + fallback 接線（`wi_ai_enabled`）
- [x] L1-3 injection / dimm / fallback 測試 + FakeLLMClient（minimal：無真模型）
- [x] L1 checkpoint
  - 驗證：unit + L1 FakeLLM/unreachable integration 綠；review → REQUEST_CHANGES 後修 P0/P1 → **APPROVE**（[L1 re-check](38db58c0-df15-4693-8e3d-5105d37d3618)）。
  - 備註：minimal 骨架；prompt few-shot 可後調；drafts 仍空（L2）；security 延後。

### L2
- [x] L2-0 端到端 fixture 5 筆（spec 附錄 A5）
- [x] L2-1 `linking.py`（L0/L1/L2 層；embedding 可後補）
- [x] L2-2 `most_compiler/` + 決策表矩陣測試
- [x] L2-3 engine gate + routing + multi_action 整合測試 + golden 全跑
- [x] L2 checkpoint
  - 驗證：A5 fixtures + engine_gate unit；integration NLP/fallback/L2 13 passed；golden 全綠；review → REQUEST_CHANGES 後修 P1 → **APPROVE_WITH_NITS**（[L2 re-check](8b77587a-1da0-46e8-9b51-62c6f70d4ccf)）。
  - 備註：L3 embedding 未做；A5 其餘 reject code 可後補；security 延後。

### L3
- [x] L3-1 `ai_review.py` + `ai_review_service`（events + candidates）+ 測試
- [x] L3-2 前端 `AiDraftPanel` 等 + typecheck/build（Playwright e2e 可後補）
- [x] L3-3 gold plans 目錄與 eval script 骨架
- [x] L3 checkpoint（實作出口；§0.1 現場五條待人確認）
  - 驗證：L3-1 review API；L3-2 typecheck/build＋APPROVE；L3-3 `test_gold_plans` 4 passed、`wi_ai_eval` 3/3、core_logic golden 全綠。
  - 缺口：Playwright e2e／A6 band／正式 IE gold 核准／資安席位／OpenAPI gen:api。
  - L3-2 備註：minimal — drafts／採用／accept_plan／stale／multi_action。
  - L3-3 備註：`tests/gold/wi_plans/` seed + `scripts/wi_ai_eval.py` + `docs/llm/eval-reports/`。

### L4（D3-007 jobs-first）
- [x] L4-0 ADR-026／027 → accepted；worklog／registry
- [x] L4-1 `ImportRow` + `AiParseJob`／`AiParseJobItem` + migration `v2_0028`
- [x] L4-2 `parse_job_service`（materialize／create／claim SKIP LOCKED／tick／cancel）+ `parse_interactive(source_kind=import_row)`
- [x] L4-3 API：`POST/GET .../parse-jobs`、`.../tick`、`.../cancel`（analyst+；不改 map/submit）
- [x] L4-4 unit：`test_parse_job_service`；integration：`test_parse_jobs_api`
- [x] L4-5 `alembic upgrade head` + integration 綠 + checkpoint review
  - 驗證：`v2_0027→v2_0028`；unit+integration **9 passed**；review [L4 WI AI code review](77df24e2-19a1-47cb-836a-6b1c174d2f79) → REQUEST_CHANGES 後修 P1 → **APPROVE**。
  - R1 revision／policy／outbox 另工（D3-007）；資安延後。
  - agent shell 無 host docker/network（BLK-003 resolved via 使用者終端）。

### R1（worksheet revision）
- [x] R1-1 migration `v2_0029`：`revision_no`／`content_hash`／`last_edited_*`；`ai_parse_runs.source_revision`
- [x] R1-2 `worksheet_revision.bump` CAS + save／from-module／submit 接線；clone／create 重置為 1；publish 不 bump
- [x] R1-3 FE：store revision、save `base_revision`、409 UX；AI adopt 擋 stale source_revision
- [x] R1-4 `alembic upgrade` + integration + typecheck + checkpoint
  - 驗證：`v2_0028→v2_0029`；unit+revision_api+worksheet **16 passed**；typecheck 綠。
  - ORM expire fix：bump 後必須清 session 快取，避免舊 revision 被 flush 蓋回。
    - ⚠️ **2026-08-12 更正並修復（P0）**：當時採用的 `session.expire_all()` 會失效
      **整個 session** 的 ORM 物件，導致 async 下呼叫端的下一次屬性存取變成
      `MissingGreenlet`。實際線上影響三處：WI Set 專案含 ≥2 筆 WI、
      `from-module` 帶 `base_revision`、`imports/submit` 帶 `base_revision`（前端實際就這樣送）
      全部 500。四個呼叫點只有 `save_worksheet` 因為之後多做了一次 `session.get`
      而僥倖能用——而那正是唯一有 CAS 測試的那一個。
    - 另更正失效機制：「舊 revision 被 flush 蓋回」實測在 SQLAlchemy 2.0 下**不成立**
      （ORM UPDATE 只寫有 history 差異的欄位）。真正可觸發的是 **stale read**——
      `read_worksheet()` 直接讀 identity map 那顆物件，回舊 revision 給 client，
      client 拿它當下次 `base_revision` → 永遠 409。
    - 修法：`session.refresh(ws, attribute_names=[...])` 只重讀被 Core UPDATE 改過的欄位。
      補 7 條迴歸測試（含 mutation 驗證）。詳見
      [引擎與 legacy 稽核 §11](../architecture/legacy-inventory-and-engine-audit.md)。
  - P0/P1 後修：AI cache key 含 ws:rev；Import/from-module `base_revision`；legacy adopt stale gate；`source_revision` BigInteger。
  - checkpoint：[R1 review](ca934a12-62e5-4670-a022-29c7602c86a7) → **APPROVE_WITH_NITS**（nit：legacy null revision；import/CAS／cache bust 測試可後補）。資安延後。

### R2a（policy manifests）
- [x] R2a-1 migration `v2_0030`：`modeling_policy_versions`／`level_policy_versions`；worksheet nullable FKs；seed＋backfill factory V1；`ai_parse_jobs` FK
- [x] R2a-2 resolver：`(MODELING_FACTORY|LEVEL_FACTORY, version_no=1, published)`；缺則 `NoDefaultPolicy`；create／clone／read 接線
- [x] R2a-3 unit `test_policy_manifest`＋integration `test_policy_manifest_api`
- [x] R2a-4 `alembic upgrade` + pytest + checkpoint（待使用者終端）
  - 驗證：`v2_0029→v2_0030`；unit+policy_api+worksheet **17 passed**。
  - checkpoint：[R2a review](52d1b490-4ada-41e8-815d-691c7fd124ed) → **APPROVE_WITH_NITS**（ORM index 已補；fail-closed 測試可後補）。資安延後。
  - **非本切片**：R2b `level_validation_runs`／publish gate／Level validate 讀 policy。

### R2b（level validation runs）
- [x] R2b-1 migration `v2_0031`：`level_validation_runs` + unique (ws, rev, policy, input_hash)
- [x] R2b-2 `level_validation_service`：validate_and_persist／assert_publishable；save／clone 寫 run；publish gate
- [x] R2b-3 `POST .../level/validate`；409/422 `LEVEL_VALIDATION_*`
- [x] R2b-4 alembic + pytest + checkpoint（待使用者終端）
  - 驗證：`v2_0030→v2_0031`；unit+level_validation_api+worksheet+revision+policy **23 passed**。
  - checkpoint：[R2b review](9b4a4c82-95cd-4bc1-a864-e1f74a03010a) → **APPROVE_WITH_NITS**（nit：policy mismatch／舊 rev 測試可後補）。資安延後。

### R3b（outbox）
- [x] R3b-1 migration `v2_0032`：`outbox_events`（pending/published/failed/dead_letter）
- [x] R3b-2 `outbox_service.enqueue_*` + claim／mark；`record_reviews`／`save_worksheet` 同交易寫入
- [x] R3b-3 unit + integration `test_outbox*`
- [x] R3b-4 alembic + pytest + checkpoint（待使用者終端）
  - 驗證：`v2_0031→v2_0033`；outbox+ai_review **8 passed**（含 UNIQUE migration）。
  - checkpoint：[R3b review](19d777d6-6a9a-49d5-bfc9-5b19dc078d20) → P1 UNIQUE 已補 → **APPROVE_WITH_NITS**。資安延後。
  - **非本切片**：R3a `wi_row_contexts`；publisher worker／AI DB role／完整 R0 retry policy。

### R3a（wi_row_contexts）
- [x] R3a-1 migration `v2_0034`：`wi_row_contexts`（UNIQUE wi_row_id CASCADE）
- [x] R3a-2 `wi-context-v1` Pydantic 校驗＋hash；GET/PUT/DELETE `/wi-rows/{id}/context`
- [x] R3a-3 unit + integration
- [x] R3a-4 alembic + pytest + checkpoint（待使用者終端）
  - 驗證：`v2_0033→v2_0034`；unit+integration **8 passed**（含 publish freeze）。
  - checkpoint：[R3a review](3325fa79-8b6a-4f87-9e0b-0b7386f6a27c) → P1 凍結測試已補 → **APPROVE_WITH_NITS**。資安延後。
  - **非本切片**：FE 編輯 UI；與 worksheet save 嵌套寫入；AI parse context 升正式欄。

## 3. Blocker Log（卡點紀錄）

> 格式：現象寫「發生了什麼」，處置寫「為什麼這樣解」。重開同一問題＝新編號＋引用舊編號。

| 編號 | 日期 | Phase | 現象 | 影響範圍 | 分級 | 處置與理由 | 狀態 |
|------|------|-------|------|----------|------|------------|------|
| BLK-001 | 2026-08-07 | L0 | 本機 Docker daemon 未啟動，無法跑 `alembic upgrade`／integration（需 DATABASE_URL） | L0 退出條件中的 migration/integration 驗收 | D3 | 啟動 docker compose db 後：`alembic upgrade head`（v2_0025→v2_0026）成功；`pytest tests/integration/test_nlp_api.py` → 9 passed。 | resolved |
| BLK-002 | 2026-08-10 | L4 | ADR-026／027 仍為 **proposed**；L4 硬條件含 accepted＋worksheet revision（R1）＋`import_rows`／`ai_parse_jobs` proposed schema | 不得開 L4 migration／worker／改 ADR-025 submit | D1 | 2026-08-10 User 核可兩 ADR → accepted。R1 revision／policy manifest／outbox 仍分批；L4 先 jobs 最小切片（D3-007）。 | resolved |
| BLK-003 | 2026-08-10 | L4 | Docker／DB：agent shell 無 sock／連不到 5432；使用者一般 WSL 可連 | L4-5 曾無法在 agent 內驗收 | D3 | 使用者終端：`alembic upgrade` v2_0028 + pytest 7 passed。agent 與 host 網路隔離保留為環境限制。 | resolved |

## 4. Decision Log（D3 實作級決策）

> D1 → `docs/decisions/ADR-*`；D2 → 修 spec＋§20.5 修訂紀錄。本表只收 D3。

| 編號 | 日期 | Phase | 背景 | 選項 | 選擇與理由 |
|------|------|-------|------|------|------------|
| D3-001 | 2026-08-07 | 規劃 | spec v1.0 七個軟點 | 邊做邊猜 vs 先查證定案 | 先查證定案，寫入附錄 A（升 v1.1） |
| D3-002 | 2026-08-07 | L0 | input_hash 用 bundle.code 或 bundle.id | code（穩定字串） vs UUID | 採用 code，與 settings/bundle seed 對齊；記於 wi_ai_service 註解 |
| D3-003 | 2026-08-07 | L0 | cache hit 時 legacy slots 重算或還原 | 重算（會與 synonym 漂移） vs 自 run 還原 | 自 run 的 slot_candidates 還原（legacy_from_run_snapshot），保證 ai.* 與 legacy 一致 |
| D3-004 | 2026-08-07 | L3 | OpenAPI nl-draft 200 為任意物件 | 等後端補 schema 再 gen:api vs 前端 `aiTypes.ts` 對齊 contracts | 採 aiTypes.ts；升 OpenAPI 後再 gen:api 並改 import |
| D3-005 | 2026-08-07 | L3 | A6 信心三檔缺後端 band 欄 | 前端重算 vs 暫不顯示 | 暫顯示「可採用／待審」；A6 band 等後端輸出後再接 |
| D3-006 | 2026-08-10 | L3 | gold 案例尚無 IE 正式核准 | 空目錄等 IE vs A5 fixture 作 `approved_by=seed` | 先 seed 3 筆跑通 eval；IE 核准後改標籤不改 TMU 鎖點（除非 rule-set 變更） |
| D3-007 | 2026-08-10 | L4 | L4 全量含 R1 revision／policy／outbox | 一次做完 vs jobs-first | User 選 jobs-first：`import_rows`+`ai_parse_jobs/items`+API+tick worker；`modeling_policy_version_id` 可 NULL；不改 ADR-025 submit；R1 另工 |
| D3-008 | 2026-08-15 | L4 | jobs 只能靠 API 手動 tick，批次 job 建了不會自己跑 | 手動 tick 維持 vs lifespan 背景 worker | lifespan asyncio task 週期驅動（`services/v2/parse_job_worker.py`；`DDM_PARSE_WORKER_ENABLED/_INTERVAL_S/_BATCH`，**預設開**——compose 不另設就會跑）；per-job 錯誤隔離＋poison 標 failed；多實例沿用 CLAIM_SQL SKIP LOCKED＋lease；API tick 保留供手動/測試。**部署拓撲面已升級 D1 → ADR-030（proposed）** |
| D3-009 | 2026-08-15 | 硬化 | 毒 job（病理長輸入）可卡死 event loop 分鐘級且重啟不復原（checkpoint security F1） | 只加 timeout vs 三層防禦 | 三層：①長度上限 `MAX_PARSE_TEXT_CHARS=2000`（=NLDraftIn 既有契約；病理最壞實測 800c≈0.18s/1600c≈1.4s，2000 鎖在 2–3s；超過標 review 不進 parser）②同步 CPU 段（normalize/rule parse）丟 executor＋`wait_for(PARSE_CPU_TIMEOUT_S=10)`（=最壞 2–3s 的 3 倍餘裕；單加 wait_for 無效——同步阻塞下 timeout 無作用點）③CLAIM_SQL 加 `attempt_count < MAX_ATTEMPTS`＋`_reap_exhausted_items` 收屍，毒 item 有終點 |
| D3-010 | 2026-08-15 | 硬化 | `_mark_job_failed` 把 pool 逾時/斷線等暫時性失敗判死刑，且繞過 `_finalize_job_status` 留下「終態 job＋active items」（code-reviewer #1） | 全部判死 vs 失敗分類 | 結構性（JobNotFound/RuleSetNotFound/RuntimeError=bundle 缺失）立即判死；其他視為 infra，log 後下輪重試、**連續** 5 次才判死（`MAX_CONSECUTIVE_JOB_FAILURES`）。判死一律走 `svc.fail_job`（收尾 items＋import_rows＋`_finalize_job_status`），狀態機單一路徑 |
| D3-011 | 2026-08-15 | 硬化 | 撤權不中止在途 job（security F4） | 完整物件級授權 vs 最小集 | 本輪最小集：`_runnable_jobs` 跳過 `app_users.is_active=False` 的 requester。**已知缺口（後續票）**：imported_by/site scope 的完整物件級授權（誰能 create/tick/cancel 誰的 import）未做 |
| D3-012 | 2026-08-15 | 硬化 | tick 交易在首個計數 UPDATE 後抱著 ai_parse_jobs 列鎖跨 LLM 呼叫，Cancel 被卡（code-reviewer #3） | job 挑選 SKIP LOCKED／LLM 移出交易 vs 縮鎖窗＋誠實文件 | 選後者：計數改交易尾端一次性遞增（鎖窗縮到 LLM 之後；殘餘＝首次 tick 的 queued→running，上限 ≈ batch×llm_timeout=32s，LLM 預設關閉時毫秒級）；worker docstring 撤回「job 挑選層免鎖」的錯誤宣稱。LLM 移出交易＝重構 parse_interactive 的交易邊界，收益只在 LLM 開啟時存在 → 併入 ADR-030 觸發條件 2（dedicated worker 抽離時做正解） |
| D3-013 | 2026-08-16 | P0 gold | gold 預標註管線（`scripts/gold_harvest.py`）：草稿 plan＝rule planner 輸出，IE 原樣轉正會讓 planner 給自己打分（審查實測 accuracy 0.667→0.98 假跳） | 指標照算＋文件警告 vs 結構性排除 | 結構性排除：草稿標 `plan_origin=rule_based_v1_preannotation`、轉正必填 `ie_modified: true\|false`；`summarize_planner_results` 把「plan_origin=受測 planner 且 ie_modified≠true」排除出 Plan 層指標（報告 `self_referential_excluded` 列名單與理由；report schema 升 v4）。橡皮圖章回歸測試釘死「未修改轉正 → 指標不動」。配套：pending_ie 守門改整份 plan 相等比對；`--recompile` 對正式 gold 預設拒絕（`--relock-approved --reason` 留痕）；「取＋放」配對不再誤標 multi_action（13/42 筆改發中性旗標 `take_place_pair_may_be_single_gm`）；非 seed 核准案例須有實質 cycle 或 `expected_incomplete_reason`；未核准案例混入 gold_dir 時報告降級 `wi-draft-latest.json` |
| D3-014 | 2026-08-16 | P0 gold | User/IE 三條裁決：①「取＋放」看動作（單一 cycle 與兩個 action 都可能，逐案裁決）②不變式「取最後一定有放」③v3 遷移資料是 IE 驗證過並提供的——結構（module/cycle 邊界）就是 IE 的切分裁決 | 全域定死配對方向 vs 逐案回填 v3 結構答案；文字啟發式 lint vs action_type 序列 lint | ①＋③：查證 cycle 容器語意（`motion_module_versions.rows[]` 一列＝一 cycle、`motion_modules.name_zh`＝發布版列數、`wi_rows` 一列＝一 cycle 容器但 v3 搬遷不寫此表、`motion_templates` 同）→ 帶切分旗標草稿回填 `v3_structure_hint`＋逐來源證據，只採 v3-import 訊號；配對題有答案改**確認題**（預設依 v3 結構，IE 可推翻——hint 是證據不是判決，絕不寫進 expected.*）。統計：19 配對筆 18 答案＋1 ambiguous（d026 同句跨結構矛盾）；22 likely_multi 筆 21 答案（17 single→「幾乎必然低估」警語降級）＋1 ambiguous（d045 唯一來源＝dev seed wi_rows）。②`acquire_without_place` lint：acquire 其後同 plan 內無 move_place/release_return/controlled_move（CM 無 P、M 即閉合，收 controlled_move 以免跟裁決①打架）→ WARN 旗標＋覆核表問「放在哪」；本輪 0 命中＝結構使然（rule adapter 永不出 acquire），`--recompile` 同步旗標供 IE 改後生效。mutation 7 條逐一紅；正式 gold 3 筆 byte 不動 |
| D3-015 | 2026-08-16 | P0 gold | IE 首輪覆核結果（User 親答）：①39 筆確認題全照 v3 結構預設 OK ②d045「拿取排線並對準接頭」＝1 列 ③d026「雙手抓握主板組至機箱」＝3 列（矛盾結構中 3 列 wi-template 是對的）。致命前提：`--force` 整批重寫草稿，覆核記錄寫在草稿上會被第二輪（同義詞登記後必重跑）洗掉 | 覆核狀態寫草稿（重產即失）vs 獨立 state 檔＋重產合併 | 獨立檔 `tests/gold/wi_plans_draft/review-state.json`（IE 的檔案：harvest 只讀不寫、`--force` 不刪），鍵＝normalized_text sha256 前 8 碼（跟句子不跟流水號）；harvest 重產時合併回草稿 `ie_review` 區塊。stale 不靜默套用（文字變＝配不到、v3 hint 變＝證據已不同、entry ie_modified=true＝plan 內容保不了→逐條列入摘要）；sha8 同但全 sha 不同＝完整性失敗大聲擋。套用結果：39 筆記 `segmentation_confirmed_by=IEC141289`＋`v3_structure_confirmed`（切分維度確認≠整筆核准；`ie_modified: false` 確認≠修改）；d045 記 `ie_ruling: single_cycle`（plan 不動）；d026 記 `ie_ruling: multi_cycle_3`＋**誠實降級** `plan_pending_resegmentation: true`——查證 3 列 wi-template（dcbb30a8）子句＝「雙手接觸DIMM壓合治具拉至規定位置／雙手抓握主板保持住至流水線／雙手抓握主板組至機箱」，非原句子字串，切不出 3 段誠實 evidence span（不編造），plan 重切等第二輪；被否定的單列 action module（32a29bba）標 `ie_ruling_rejected` 證據保留。配套：同義詞候選清單 `docs/llm/gold-review/synonym-candidates.md`（23 動詞面：10 同字直配＋13 標 `?` 待 IE 裁決；**候選未寫 DB**，ADR-023 治理）。驗證：--force 重跑 41 筆覆核記錄 byte-identical 存活；unit 656／integration 446＋1 skip／golden 全綠／eval 仍 n=3；正式 gold 3 筆不動 |
| D3-016 | 2026-08-16 | P0 gold | IE 第二輪指示（User 親答）：①d026「雙手抓握主板組至機箱」**這句本身＝1 列**——首輪「3 列」是對整個三步驟製程（wi-template）的回答，提問誤述範本結構為句子切分，釐清後更正 ②10 個同字直配同義詞核可先登記（含推至/拉至共 11 條映射），13 個 `?` 不登等具體句子情境 | 更正無痕覆寫 vs 保留軌跡；同義詞走 API vs 直寫 DB | ①`review-state.json` d026 entry 改 `ie_ruling: single_cycle`、清 `plan_pending_resegmentation`；**更正軌跡**存 entry `ruling_history`（先前 multi_cycle_3 全文＋supersede_reason；`load_review_state` 加軌跡形狀硬驗證——空殼軌跡擋下）；被否定結構改為 dcbb30a8 name_zh 的「名稱句→3 列」推論（本句自己是該範本 rows[2] 一列＝一 cycle），首輪對 32a29bba 的否定撤回 ②走正常 API `POST /api/v2/rule-sets/MINIMOST_FACTORY_V2/synonyms`（analyst+；登記人 IEC141289；priority 0）＝11 條，唯讀查表確證恰 11 列 ③第二輪 harvest `--force`：41 筆覆核狀態全存活（stale 0）、決定性二跑 byte-identical、正式 gold 3 筆不動。**誠實回報：cycle 完成度 0/60**——11 條同義詞讓 32/60 筆拿到 slot 命中（`empty_lexicon_no_slot_candidates` 60→0），但 rule_based_v1 的 GM/CM 判型只認名詞觸發詞（治具/機台）不吃詞典：50 筆卡 `composite_unknown`（39 筆句含 `?` 動詞、11 筆句面動詞全已登記＝判型是唯一卡點）；10 筆 typed GM 全 `missing_core_p`（3 筆卡未裁決的 `?` P 動詞「放至/放置」——實驗證實登記即轉 complete；7 筆句面無 P 動詞，需 IE 改 plan 非同義詞可解）。判型吃詞典與否＝rule parser 設計決策，未動核心（另票）。守門：slot 命中基線 superset 釘＋complete 集合等值釘（`test_gold_draft_schema.py`）、`ruling_history` 驗證 mutation 測試 |
| D3-017 | 2026-08-16 | P0 gold | IE 第三輪裁決（User 親答）：①「放至/放置」按情境——放入**機構件**（治具/卡槽/機箱這類要定位的）→必對準；放上**盤面類**（盤子/conveyor 類，含流水線/工作台/垃圾桶）→無方向；方向數 IE 未指定→對準情境預設一種；「組至/組於/插入」未裁維持 `?` ②核可「判型吃字典」：GM/CM 判型從只認名詞觸發詞擴成動詞字典命中參與 | 一面一 code 維持（登不了兩變體）vs 放寬 UNIQUE 一面多 code；判型全域動詞壓名詞 vs 分象限衝突矩陣＋棄權 | ①走同一 API 登記「放至/放置」各兩條（`p_place_single` priority 0＝預設、`p_place_none` priority 1；11→15 條，逐條 created_by=IEC141289）。**發現既有 UNIQUE(rule_set_id,parameter,synonym_norm) 使 priority 欄形同虛設**（一面永遠只有一列，「同面多 code 排序」無從發生）→ migration **v2_0038** 放寬為含 option_code（downgrade 附、破壞性清理有記載）；priority 定調＝偏好位次（0＝預設），lexicon tie-break（`nlp/lexicon.py`）＋list/trgm 排序對齊。**scope 偏差留痕**：migrations/models/synonym_service 三檔不在派工範圍——不放寬則任務 1 的 4 條登不進去（衝突回查含 option_code 是新約束下的必要修正）。方向變體選擇＝賓語情境規則（`nlp/linking.py` `classify_p_destination`：機構件/盤面名詞清單單一出處、逐項附語料證據、子句標點截尾；生產 nl-draft／gold_eval 重放／harvest 同一套，無平行路徑）②`classify_seq` 衝突矩陣：M 命中＝CM 訊號（M 是 CM 專屬核心參數；G 可伴隨＝IE 已裁「接觸+推/拉」單一 CM）、G+P 組合＝GM 訊號、**M+P 同句＝跨模型混合→棄權（蓋過名詞）**、名詞 GM×動詞 CM→CM（d019 型，動詞在此象限對）、**名詞 CM×動詞 GM→棄權**（無裁決不硬判——非全域動詞壓名詞）、動詞無訊號→名詞判原樣保留；判型與 slot 填充共用同一次 match_all（不另開查詢路徑）。第三輪 harvest：typed 10→29（GM 13＋CM 16）、complete 帶 TMU 0→27（**9 筆 TMU=0.0**——距離未述＝0cm＋linker 只掛 core 參數，complete≠可信 TMU，摘要誠實旗標）；判型未定 31 筆逐類（27 卡 `?` 動詞、1 跨模型混合、1 單動詞不足、2 無動詞面）；24 筆判型變更逐筆標「第三輪判型已修正，請確認」（`typing_change` 舊/新值可重算，schema 守門）；P 方向數旗標 6 筆（3 機構件 single／2 盤面 none／1 判不出）。41 筆覆核狀態 `--force` 存活（stale 0）、決定性二跑 byte-identical、正式 gold 3 筆不動。planner eval 正式 gold n=3 分數**不變**（0.6667／0.5714）——Plan 層指標只量 action 數與 boundary span，判型不在其中，且 3 筆 seed 案例無已登記 M 面/G+P 組合；判型進步體現在草稿集分佈，誠實記錄不假造分數變化。mutation 9 條逐一紅（矩陣各象限/棄權/棄權拆除/lexicon tie-break/情境規則反向/unclassified 不標 review/名詞源拆除→宣稱重算失配） |
| D3-019 | 2026-08-17 | P0 gold | IE 第三輪答案（User 親答）：①「拿取」→ 預設「選取」②24 筆判型修正照預設（確認）③6 筆放置方向數照預設 ④9 筆 TMU=0.0 照預設——協調者解讀為「判定句子資訊不足」（**已向 User 揭示待確認**）。同時執行首批正式 gold 轉正 | 覆核答案寫草稿（重產即失）vs review-state 面向化；轉正手動搬檔 vs 工具化資格檢查 | ①走同一 API 登記「拿取→g_pick_sel」（G，priority 0，IEC141289）**恰一條**——small/collect 變體 IE 未授權都掛，維持候選（詞典 15→16 條，查表逐條確證）②③④review-state entry **面向化**（wi-review-state-v1 加法擴充）：判型（typing_confirmed_by/date＋typing_change_at_review）、P 方向（p_direction_*＋caveat_at_review）、TMU=0 裁決（zero_tmu_ruling="distance_unstated"＋ruled_by/date→合併時草稿記 `expected_incomplete_reason`，走 D3-018 M1 誠實記錄路徑，不發明距離）；`*_at_review`＝stale 判定基準（typing_change_changed／p_direction_context_changed／zero_tmu_flag_absent，entry 級 all-or-nothing 不部分沿用）；**scope 註記**：任務原文寫 `ie_ruling: "distance_unstated"`，實作為獨立欄 `zero_tmu_ruling`——`ie_ruling` 是切分裁決欄（硬 regex single/multi_cycle＋「確認≠裁決」不變式），重載會打破 41 筆既有 entry 的驗證，語意不變留痕。3 筆不在首輪 41 內的草稿（d033/d042/d049）新增 entry 僅帶判型/TMU 面向（切分 IE 未答不冒填）。第四輪 harvest：詞典 11→15→16，typed 29→33（GM 13→17）、complete 27→31（TMU=0.0 恆 9 筆、全數已裁）、判型變更旗標 24→28（新 4 筆未確認）、P 方向 6→8（新 2 筆未確認）；44 entry 全套用 stale 0、決定性 byte-identical、harvest 階段正式 gold 3 筆不動。⑥**首批轉正 21 筆**（`--promote` 新子命令；資格檢查 promotion_blockers 唯一出處：切分已確認＋結構與 plan 一致＋旗標全解＋TMU>0 或 reason＋S 檢；整批先驗、compile/planner 預檢全綠才落筆）：g06–g26，split **全部 test**（unique 描述不足以三分、test 只收 IE 核准；challenge-oversampled 但書寫進每檔 notes）、approved_date=2026-08-17、`ie_modified: false` 全數維持；14 筆 TMU>0＋7 筆 expected_incomplete_reason。**被擋 39 筆逐類**：multi_cycle 確認但 plan 未重切 6（d001/d005/d008/d012/d016/d030）；新 typed 判型/方向未確認 3（d015/d029/d039）；切分未確認（entry 僅它面向）3（d033/d042/d049）；不在 41 筆內（無 entry）16；已確認但 incomplete 無實質（卡 `?` 動詞等）11。state entry 標 promoted_to/promoted_date（軌跡保留不刪、merge 跳過）。釘值：SEED_GOLD_N=3 不動、新增 PROMOTED_GOLD_N=21/GOLD_TOTAL_N=24；planner 指標**維持 0.6667/0.5714**（21 筆全數自我指涉排除，plan_metrics_n=3——橡皮圖章防線實證）；eval n=24、compile 24/24 綠、報告 wi-gold-latest（dataset_note 帶「不計 planner 段證據力」但書）。mutation 4 條紅→綠（資格檢查拆除→6 紅含 repo tripwire／promoted 標記拆除→雙向比對紅／substance >0 退回 is not None→紅／typing stale 檢查拆除→紅） |
| D3-018 | 2026-08-16 | 硬化 | code-reviewer 對 D3-017 的複審。必修四條＋ADR 補節：**H1** v2_0038 放寬 UNIQUE 拆掉治理防線無替代品——同面兩 code 撞同一 priority DB 不擋，parser tie-break（(priority, option_code) 字母序）**靜默擇一、needs_review=False、另一 code 不進 top_k**（實測 g_grasp/g_touch 同掛「握住」priority 0 → 永選 g_grasp，TMU 錯且無旗標）；**M1** TMU=0.0 穿透空殼守門（`total_tmu is not None` 讓 0.0 算實質內容）且覆核表對 9 筆 TMU=0.0 無逐筆警語；**M2** 兩處權威文件不實（CI_GATES:84「治具→GM」已非生產行為——15 條詞典下「接觸治具拉至」→CM；impl-spec:79 仍寫舊 UNIQUE）；**M6** linking.py docstring「一律留 review 旗標」生產端為假（機構件分支 needs_review 通常 False） | H1：DB 約束 vs service 層擋同 priority 撞面；top_k 變體注入通則化本輪做 vs 記票 | **H1 採 (a)**：`create_synonym` 擋「同 (rule_set, param, norm) 已存在**其他** code 且 priority 相同」→ 409 `SYNONYM_PRIORITY_COLLISION`（訊息明說「一面多 code 需以不同 priority 顯式宣告偏好序」；既有 15 條不違反、p_place 變體 0/1 不受影響；**殘留**：service 層無 DB 約束背書、並發可穿透＋top_k 通則化——記後續票，ADR-023 §3.3 規則 1 補節記載）。**M1**：空殼守門改 `total_tmu > 0`（`expected_incomplete_reason` 路徑照樣放行）；`zero_tmu` 升 per-draft 旗標 `zero_tmu_distance_unstated`＋覆核表對 9 筆逐筆問「此句未述距離，TMU=0.0 非真值——補距離或判定句子資訊不足」（單一出處 `has_zero_tmu_complete_cycle`；schema 5g 同進同出守門；草稿重產）。**M2/M6 照修**；**ADR-023 §3.3 規則 1 補節**（新 UNIQUE 定義／priority 語意／H1 殘留風險與補救／P 方向名詞清單暫住程式碼的理由與畢業條件；ADR 狀態不動）。**記票不實作**：M3 幽靈命中守衛上移生產（「壓合」登記為同義詞**前**必須先做——該登記動作的前置條件）；M4 棄權安全網與詞典完整度綁定的措辭；M5 `p_direction_unclassified` 進 routing_reasons；M7 `list_synonyms` 排序讓同面變體相鄰；L1 top_k 注入變體擠掉第 5 名；L2–L4 其餘低風險小修（見複審紀錄）。驗證：unit 683／integration 449＋1 skip／golden 全綠／eval n=3 分數不變；41 筆覆核狀態存活（stale 0）、決定性二跑 byte-identical、正式 gold 3 筆不動、DB 仍 15 條；mutation 4 條逐一紅→綠（H1 守門拆除／空殼守門回退 is not None／preannotate 旗標拆除重產→schema 5g 恰 9 筆紅／覆核表提問拆除） |
| D3-021 | 2026-08-17 | P0 gold | IE 第四輪答案（User 親答）＋第二批轉正：①確認→範圍內 `i_confirm` ②鎖附→X 製程 `x_screw_fix`（電動起子情境；手動鎖附遇到再議）③組至/組於→`p_asm_single`（照放置邏輯，方向數逐筆確認）④清潔→`x_blow_clean` **僅吹風情境** ⑤插入＝多費力：P 主項 `p_asm_single`＋既有 addon `a_insert` **兩段都計時**（IE 對雙重計價懸案的正式裁決，candidates 文件標記已解）⑥4 筆新旗標（a062c017/d0350279/650ee42f/0423b4e8）照預設確認 ⑦16 筆無爭點案例批次切分確認。重切 6 筆（D3-020 建議稿）IE 未答——本輪不動、維持被擋 | 清潔無條件登記 vs 情境守門；批次確認併入 v3_structure_confirmed vs 第三來源值；新 TMU=0.0 沿用上輪解讀 vs 標旗不轉正 | ①**6 條同義詞走同一 API 逐條 201**（IEC141289、priority 0）、唯讀查表確證 **22 條**。**清潔情境守門**：DB 映射是全域的、IE 裁決是情境的——harvest 掛 `x_clean_context_unverified`（單一出處 `x_clean_context_missing`：lexicon 配出映射且句面無風槍/吹風；仿 p_direction 模式，schema 5i 同進同出守門、轉正 fail-closed），**不無條件套用**；本輪 0 命中（語料 2 筆清潔全在風槍脈絡）②review-state 落答案：4 筆判型確認（typing_change_at_review 釘當時值）＋2 筆 P 方向；**批次確認＝切分來源第三值 `no_contention_batch_confirmed`**（與逐筆確認可區分——批次未逐筆核 v3 結構證據；驗證：hint_at_review 必 null、不得帶 ie_ruling；合併前提：草稿長出切分旗標 ⇒ `segmentation_contention_appeared` stale；結構一致性＝single_cycle）；`ie_modified` 全 false；entry 44→60 ③第五輪 harvest `--force`：60 entry 全存活（39 active 套用＋21 promoted 跳過、stale 0）、決定性二跑 byte-identical、**正式 gold 24 筆 byte-identical（新增不變式：轉正 gold 一經寫入不得被重產動到——本輪驗證通過）**。草稿集換血：已轉正 21 句移出、21 筆新候選補位（60 維持）；續留 39 句 typed 12→19（**組至/組於/插入解鎖 7 筆 GM complete 帶 TMU**）、詞典 16→22；五輪對比（詞典 0→11→15→16→22；complete 0→0→27→31→19，第五輪口徑=換血後全集）。**新出現 TMU=0.0 恰 1 筆**（`7f085e02` 撕除螢幕保護膜）——上輪 9 筆「資訊不足」解讀仍待 User 明示確認、**不擴大適用**：標旗列清單、無 entry、本輪不轉正 ④**第二批轉正 3 筆**（資格檢查 D3-019 全套；compile/planner 預檢綠才落筆）：a062c017→g27_pick_dimm_box_to_bench、650ee42f→g28_pick_board_hold、0423b4e8→g29_pick_dummy_dimm_hold_to_line（split=test、approved 2026-08-17、ie_modified false）＝D3-019「新 typed 判型/方向未確認」3 筆本輪確認後解鎖。**任務預期「B 的 16 筆」＝0 轉正——誠實回報**：批次確認只解切分維度，16 筆全數 incomplete 無 TMU 卡實質內容守門（X/I 命中不參與判型＝D3-017 矩陣既定範圍，鎖附/確認/清潔解鎖不了 complete）。**被擋 57 筆逐類**：已確認但 incomplete 無實質 21（批次 16＋既有確認 5）；新進無 entry 21（d040–d060，含 d052 新 TMU=0、d058 新判型旗）；動詞解鎖新判型旗未確認 6（7c6eb8af/b6ee694d/af172fd9/1c27dc35/e945e29e/9c1a987f）；multi_cycle 確認未重切 6（維持 D3-020 名單，其一 fe1f3a90 兼卡新判型旗）；切分未確認（entry 僅它面向）3（51518399/1dd7c1d5/6be614c5）。累計 **IE 核准 24/50**。釘值文件化：PROMOTED_GOLD_N 21→24（GOLD_TOTAL_N=27）、COMPLETE_TMU_SHA8_BASELINE 換第五輪等值集（16）、SYN_COVERAGE 基線重錄（40 筆）、切分確認守恆 15＋24＝39、repo 批次釘 16。**planner 指標維持 0.6667/0.5714**（plan_metrics_n=3；24 筆全數自我指涉排除）、compile 27/27。mutation 6 條逐一紅→綠（清潔旗標 call 拆除／情境判定拆成無條件套用／拆成無條件標旗（repo 5i d010/d012 紅）／批次合併前提拆除／批次結構一致性分支併回 hint／批次 load 驗證分支拆除）。unit 749／integration 449＋1 skip／golden 全綠 |
| D3-023 | 2026-08-17 | P0 gold | IE 第六輪答案（User 親答）：①X/I 參與判型**核可開票** ②TMU=0.0 看狀況逐筆判——產距離裁決表、不做一刀切 ③判型新旗標（含 d030）照預設確認。生產 parser 變更（`nlp/rule_based.py`，會被 code-review） | X/I 收進 CM 訊號 vs 維持只認 M；衝突象限逐格重推 vs 全域動詞壓名詞；d0350279 entry 因判型變更 stale——放寬 stale 判定 vs 誠實釘已知名單 | ①**判型訊號源擴充**：`_CM_CORE_PARAMS = {M, X, I}`（X/I 只存在 CM 序列——spec §2 CM＝A B G M X I A、GM＝A B G A B P A 無 X/I，面命中是結構性 CM 證據與 M 同級）。**衝突矩陣逐格**（逐格理由入 classify_seq docstring）：X/I 單獨（或伴 G）→CM（G 兩序列都有、CM 自有 G 格——「抓握風槍＋清潔」＝單一 CM，正式 gold g13 佐證；**X/I＋G 不是混合**）；X/I＋M 或 X＋I→CM（同向：M/X/I 本就同一 CM cycle 共存）；名詞 GM × X/I→**CM 動詞勝**（※1 擴充：「鎖附治具」＝對治具做工具製程，治具名詞只是操作對象證據、X/I 是序列參數證據——與 d019 M 裁決同一論證結構；佐證＝IE 登記 x_screw_fix 的裁決情境即電動起子工具製程（D3-021 答②））；名詞 CM × X/I→CM（同向）；X/I＋P→**mixed 棄權蓋過名詞**（跨模型：X/I 只在 CM、P 只在 GM——「鎖附後放至」型照 M+P 前例）；名詞 CM × 動詞 GM→棄權不變（※2）；空詞典回歸不變式保持（X/I 未登記＝訊號恆無＝舊行為，測試釘）。`unknown_block_reason` 改用生產端 `_verb_seq`（原自寫 has_m/has_p 在 X/I 擴充後＝平行判定路徑，拆除）。**生產 nl-draft 行為變更逐條**（現行 22 條詞典下）：(a) 鎖附/確認/清潔為主動詞的句子 suggested_seq null→CM（plan action composite_unknown→controlled_move；X/I slot 不由 linker 掛值——per-action linking 只掛 core M，cycle 為 incomplete missing_core_m，**判型解鎖≠complete**）——語料實例：草稿 9 筆＋已在正式 gold 的 1 筆（g02「拿取電動起子，依圖示鎖附兩顆螺絲」，手工 seed、凍結 plan 不受影響——eval 重放吃檔內 plan 不重跑 parser，複審查證零下游影響） (b) 治具名詞＋X/I 動詞句 GM→CM（※1 擴充；語料 0 筆、單元測試釘）(c) X/I＋P 混合句判型→棄權（誠實降級）——語料恰 1 筆＝d0350279「拿取風槍清潔放置DIMM材料盒的DIMM」G＋X＋P 混合，原 GM complete（P 面誤配定語「放置DIMM材料盒的DIMM」）本是誤判且 IE 已裁標題句不硬切，棄權更誠實；其 entry 判型面向依據消失→**stale（typing_change_changed）恰 1 筆**，照 D3-019 設計不靜默沿用，已知名單釘 `REPO_KNOWN_STALE`（多一筆紅=未預期 stale、少一筆紅=IE 已重看未清名單），列下輪快答；COMPLETE_TMU 基線有意識移除 d0350279（mutation：偷加回→紅）②**判型旗確認落地**：9 筆入 state entry 判型面向（IEC141289，2026-08-17）——7 筆 null→GM（7c6eb8af/b6ee694d/af172fd9/1c27dc35/fe1f3a90/e945e29e/9c1a987f）＋2 筆 null→CM（7f085e02/2e7b2e5a，不在首輪 41 內＝entry 僅帶判型面向不冒填切分，D3-019 前例）③**第六輪 harvest `--force`**：草稿 60→（轉正後）56；applied 33→26、stale 恆 1（d0350279）、promoted 28→35 跳過；決定性二跑 byte-identical。判型分佈（56 筆終態）：typed 16（CM 14＋GM 2）/unknown 40（mixed 棄權 1＋未登記動詞 14＋單動詞不足 20＋無動詞面 5）；**任務預期「16+ 筆判型解鎖」誠實修正＝X/I 解鎖 9 筆＋誠實降級 1 筆**（語料 X/I 面句共 10 筆，不湊預期數字）；X/I 判型上線**新增 TMU=0.0＝0 筆**（X/I 型 cycle 是 incomplete 非 complete+0.0）④**第三批轉正 7 筆 g34–g40**（判型旗確認解鎖的取放/取組配對 GM，TMU 16–24；split=test、approved 2026-08-17、ie_modified 全 false；**d030/fe1f3a90→g38 本輪過**＝D3-022 被擋原因（判型旗）已解）；累計 **IE 核准 35/50**。**被擋 56 筆逐類**：已確認但 incomplete 無實質 12；無 entry 29（含 pool 補位 3）；X/I 新判型旗未確認 9（b49a90ee/ee5c168e/51077fd1/bc473698/3791550c/7ff8b879/2f0cb396/130bb1ad/323b04c1——下輪快答）；切分未確認（entry 僅它面向）5（含 7f085e02 兼卡 TMU=0 未裁、2e7b2e5a）；d0350279 標題句 fail-closed 1 ⑤**距離裁決表** `docs/llm/gold-review/distance-rulings.md`：11 筆逐筆問「補典型距離幾 cm 或維持資訊不足」（任務稱 10 筆，實數 11——9 已轉正（7 首批＋g30/g32 重切單 cycle）＋1 已裁草稿 51518399＋1 未裁草稿 7f085e02）；不預填答案；改判流程（草稿 ie_edited／gold --relock-approved 留痕）寫在表頭 ⑥釘值：PROMOTED_GOLD_N 28→**35**（GOLD_TOTAL_N=38）；planner 指標**不動＝0.3333/0.2353**（plan_metrics_n=6、excl 25→32、compile 38/38 綠）——誠實說明：Plan 層指標只量 action 數與 boundary span，rule planner 恆單 action＋整句 evidence，判型變更影響不到這兩者，分數不動是結構必然非測不到；切分確認守恆改四項對帳 4（草稿）＋30（promoted）＋4（superseded）＋1（已知 stale）＝39。mutation 8 條逐一紅→綠：X 拆出 CM 訊號（3 紅）/I 拆出（2 紅）/X-I+P 混合棄權拆除（2 紅）/X-I+G 誤當混合（1 紅）/※1 改名詞勝（3 紅）/unknown_block_reason 退回只認 M 的平行判定（1 紅）/基線偷加回 d0350279（1 紅）/已知 stale 名單清空（2 紅）；教訓：mutation 與還原同秒且同 byte 長度會讓 pyc 快取失效判定失準（(mtime,size) 驗證），還原後須清 `__pycache__` 再驗。unit 784／integration 449＋1 skip／golden 全綠／`most_engine/` 零改動；seed 3＋既轉正 28 筆 byte-identical（git 零 diff） |
| D3-024 | 2026-08-17 | P0 gold | IE 第七輪答案（User 親答）三項裁決落地＋linker 掛 X/I 格（生產變更）：①9 筆 X/I 新判型旗照預設確認（null→CM；D3-023 快答清單名單）②d0350279 標題句 stale 重看＝**維持「不硬切、不轉正」**（裁決內容同 D3-022、重確認 2026-08-17）③**「鎖附」一律當電動（X/CM），無條件**——User 確認產線無手動鎖附，D3-021 答②「手動鎖附遇到再議」條件解除 | 鎖附建情境旗標 (c) vs 無條件裁決 (a)——User 明選 (a)，**取捨與風險記錄**：無條件裁決把「電動」假設燒進判型與掛值，未來若出現手動鎖附工位（人工扳手/手旋螺絲），「鎖附」句會被錯判 CM＋錯掛 x_screw_fix 而**無旗標可擋**，屆時需回頭補情境守門（本輪不預蓋 YAGNI）；stale entry 放寬判定沿用 vs 面向退場留痕——選後者；X/I 掛值後 completeness 放寬 vs 照擋——照擋（紅線） | ①9 筆 entry 落 typing 面向（IEC141289、2026-08-17、typing_change_at_review={null→CM}）：b49a90ee/ee5c168e/51077fd1/bc473698/3791550c/7ff8b879/2f0cb396/130bb1ad/323b04c1 ②d0350279 **依規則重新記錄**：判型與 P 方向兩面向的依據隨判型棄權消失（無旗標可「確認照預設」）→ 兩面向**退場**入 `ruling_history` 新型條目 `superseded_aspects`（退場面向原值全文保留、不無痕刪除；load_review_state 驗證 fail-closed——兩型不得混記、空殼擋、切分面向不可退場）；resegmentation_ruling 不動、notes 附重確認記錄；已知 stale 名單 REPO_KNOWN_STALE {d0350279}→**{}**（少一筆＝IE 已重看、名單同步清）；切分確認守恆 4+30+4+1→**5+30+4+0**＝39 ③`rule_based.py` classify_seq ※1 情境限縮註記改為無條件裁決（引 D3-024）；README 快答清單 1–3 題標已裁 ④**linker 掛 X/I 格**（`nlp/linking.py`）：CM 系 action（controlled_move/process/inspect）X/I **面命中才掛**進 x4.x_code／i5.i_code（與 B 條件式追加同構、無命中不加 no_candidate 噪音；GM 系不掛——GM 序列無 X/I 格）；**清潔情境守門判定單一出處搬進 linking.py**（`x_clean_context_missing`＋常數；gold_harvest import 再匯出，schema 5i／轉正 fail-closed 語意不變），掛值語意＝**旗標情境掛值照掛但 needs_review**（x_clean_context_unverified）。**生產 nl-draft 行為變更逐條**（現行 22 條詞典）：(a) CM 句含鎖附/確認/清潔面 → 對應 X/I slot 出現候選並落 cycle x4/i5（先前 controlled_move 只掛 M——IE 登記的三條映射閒置至此）；(b) 清潔（無風槍/吹風脈絡）→ X 掛 x_blow_clean＋needs_review（先前 process 核心格掛值**無旗標**——此為守門補強）；(c) 未來「M 面＋X/I 面」同句的 complete CM cycle TMU 將含 X/I 段（本輪語料 0 筆此型——現存 5 筆 complete 草稿皆無 X/I 面，COMPLETE_TMU 基線不動）；(d) process/inspect action 的非核心伴隨格（I／X）面命中同樣補掛。**完整性紅線遵守＝第四批轉正 0 筆（誠實回報）**：9 筆 X/I 句掛值後全數仍 incomplete `missing_core_m`（句面無移動動詞、M 核心格空）——「X 承載做工時 M 可為零」是 IE 域判準，**不自行放寬**，列 README 快答清單新題；被擋 56 筆逐類＝已確認但 incomplete 無實質 21（含該 9 筆）／無 entry 29／切分未確認（entry 僅它面向）5（51518399/1dd7c1d5/6be614c5/2e7b2e5a＋7f085e02 兼卡 TMU=0 未裁）／標題句 fail-closed 1（d0350279）。第七輪 harvest `--force`：applied 26→**27**（d004 回歸）、stale **0**、56 筆草稿決定性二跑 byte-identical、diff 恰 11 檔（9 筆 typing 投影＋d004＋state）；**正式 gold seed 3＋既轉正 35 筆 byte-identical（git 零 diff）**——複審查證：既轉正 gold 僅 g02（process 核心 X 已掛）與 g31（a1 inspect 核心 I 已掛；a2 evidence 無 X/I 面）帶 X/I synthetic_synonyms，掛值變更零重放影響。planner 指標**不動＝0.3333/0.2353**（plan_metrics_n=6、excl 32）、compile **38/38**、P0 累計 **35/50**。mutation 8 條逐一紅→綠：X 掛值拆除（5 紅）／I 掛值拆除（3 紅）／清潔守門 call 拆除（2 紅）／守門改「不掛值」（2 紅——掛值照掛是規格）／守門改無條件標旗（1 紅）／GM 也掛 X/I（1 紅）／completeness 放寬 X 當 core（1 紅＝紅線測試）／superseded_aspects 驗證拆除（3 紅）；每輪還原後清 `__pycache__` 再驗（D3-023 教訓）。unit **804**（790→804）／integration 449＋1 skip／golden 全綠／ruff 綠／mypy 62 errors＝基線零新增。**（2026-08-17 複審 M2 補列——行為變更清單漏列四項，補齊如下）**：(e) seconds 模式 X（清潔→x_blow_clean）掛值的 invalid 路徑與修後行為——原掛進 chosen 會讓 complete cycle 被引擎 `X_SECONDS_REQUIRED` 硬拒 → routing=invalid（「按壓把手並清潔卡槽」實測整筆判死）；D3-025 修後＝不落 chosen（X0）＋`x_seconds_required` 旗標；(f) controlled_move/process 的伴隨 I 格掛值原**無** `i_range_assumed`（守門綁死 action_type=="inspect"，「按壓DIMM壓合治具的把手並確認到位」實測 NORMAL 假設 9.0 TMU 直接 auto）；D3-025 修後＝任何 I 掛值無 inspect_kind role 皆帶旗標；(g) DB 路徑（session＋rule_set_id）下新 X/I 格同跑 L2 trgm——`engines_disagree` 多了 X/I 兩個來源格；(h) 新格以 `synonym_longest` 命中時會擋 auto（`_eligible_auto` 要求全 `synonym_exact`）——原可 auto 的句子因新掛格降級 review |
| D3-025 | 2026-08-17 | 硬化 | code-reviewer 對 D3-024 的複審。必修：**H1** seconds 模式 X 掛 chosen → 引擎硬拒 `X_SECONDS_REQUIRED`，合法草稿整筆 invalid（「按壓把手並清潔卡槽」HEAD＝complete 3.0 auto，掛值後 invalid）——語料 X/I 句全卡 `missing_core_m`（incomplete 不進 engine gate），「complete=True 才觸發」的失效在 56 草稿＋38 gold 上零覆蓋＝結構性失明；**H2** I 格掛值繞過 `i_range_assumed` 守門（守門綁死 action_type=="inspect"；i_confirm 6 vs i_confirm_out 16 TMU 的 NORMAL 假設無旗標，「按壓DIMM壓合治具的把手並確認到位」實測 9.0 TMU 直接 auto）；**M1** superseded_aspects 驗證只到鍵名（garbage 日期／非法 seq／半套面向／假退場全 ACCEPTED——entry 本體同樣壞值全被拒）；**M2** D3-024 行為變更清單漏列四項；CI_GATES 缺掛值合成句閘門 | H1：候選丟棄 vs 不落 chosen＋旗標（協調者裁定後者——資訊保留、需求浮上、不硬拒）；I 守門維持 inspect 綁定 vs 解綁；M1 另寫一份驗證 vs 重用同一批 validator | **H1**：X 面命中先查 option 的 input_mode（`_apply_x_input_mode_rule`）——fixed（x_screw_fix，fixed_seconds 有值）照掛 chosen 真 TMU；**seconds 且無秒數來源**（判準＝compile 讀 x_seconds 的同一形狀：process_kind role 帶秒值，單一語意不另創）→ 不落 chosen（cycle X 格維持空＝X0、今天行為不變）、候選仍進 top_k、掛 `x_seconds_required`——秒數模式的製程時間本來就要 IE 量測給值，不落 chosen 是誠實而非丟資訊。mode 來源：生產路徑讀 DB `rule_x_options`（runtime 值權威、查詢失敗不吞錯）；無 session 路徑（unit／gold_eval）用 seed `build_from_seed_v2`（同路徑 engine gate 的同一份資料）。守門順序＝input_mode 先於清潔情境（先定掛不掛，情境旗標只對掛上的值有意義）。修後「按壓把手並清潔卡槽」＝complete 3.0（A0 B0 G0 M3 X0 I0 A0）routing=review＋旗標在。**H2**：解綁——任何 I 格 chosen 且該 action 無 inspect_kind role → `i_range_assumed`（`_apply_i_range_rule`，與 P 方向／X 清潔守門同層）；inspect 原路徑不變（有 role 帶 text 不標旗）。修後「按壓DIMM壓合治具的把手並確認到位」＝9.0 TMU、I 帶旗標、auto→review。**M1**：退場記錄依面向分組跑**同一批** `_validate_*_aspect`（單一出處不另抄）＋「退場鍵不得同時存在於 entry 本體」（退場＝搬移非複製）；repo 現存 d0350279 退場記錄通過新驗證（零誤殺）。**M2 照補**（D3-024 列 (e)–(h)）；**CI_GATES 規則 8**：動 linker 掛值必附「核心格可填＋新格面命中」合成句跑完整 engine_gate（`test_engine_gate_*` 三條）。**記票不實作**：**L1** `_apply_x_clean_context_rule`／`x_clean_context_missing` 的脈絡輸入不同源（lexicon 配對驗 X 格 query、脈絡詞驗整句 normalized_text——docstring 有說明但兩文本可分岔）；**L2** 單一 `review_reason` 欄位先到先得——後掛旗標被既有 reason 吞（同 D3-018 M5 類；如 x_seconds_required 掛上後 x_clean_context_unverified 無處落）；**L3** X 清潔守門對 X pool 重掃 `match_all` 違反單次掃描原則（D3-017 定調判型與 slot 共用同一次掃描）。mutation 4 組逐一紅→綠：input_mode 判斷拆成 passthrough（3 紅含 engine_gate 轉 invalid）／I 守門綁回 inspect（3 紅含 engine_gate 轉 auto）／M1 分組值驗證拆除（5 紅）／假退場檢查拆除（1 紅）；每輪還原後清 `__pycache__` 再驗。unit **817**（804→817）／integration 449＋1 skip／golden 全綠／compile **38/38**／planner 指標不動 0.3333/0.2353／ruff 綠／mypy 62＝基線零新增；harvest 決定性二跑 byte-identical **且與 repo 草稿逐 byte 相同（零重產需求）**；正式 gold seed 3＋既轉正 35 筆 git 零 diff |
| D3-020 | 2026-08-17 | P0 gold | **準備中**——第五輪覆核材料：D3-019 被擋類別「multi_cycle 確認但 plan 未重切」6 筆（d001/d005/d008/d012/d016/d030）的重切建議稿已備 `docs/llm/gold-review/resegmentation-proposals.md`（唯讀產出：v3 module 列結構 DB 查證＋逐列引擎重放 TMU＋子句現行管道唯讀演算；SIMO 列不計合計交叉驗證；19 個 v3 列文字全數已是獨立草稿/轉正 gold 的對應表）。誠實邊界：d008 兩段 1:1 乾淨 span；d001/d005 各缺 1 列 span、d012/d030 第 2 列 span 不可得、d016 全 5 列 span 不可得（d026 型）——不編造，逐節列 IE 是非題與取捨點 | 建議稿先行 vs 直接改草稿 | 僅產文件與本行 worklog——未動草稿/review-state/DB/src；IE 答案落地（ie_edited→--recompile→--promote）另記 |
| D3-022 | 2026-08-17 | P0 gold | IE 重切裁決落地（User 親答）：**d008 照切、d016 不硬切、其餘（d001/d005/d012/d030）照 D3-020 建議稿**（含取捨點結論：F3 距離未述不發明/不回填 v3 值、F4 acquire 單獨子句照 g01 先例不借 p_hold、span 字面優先（去除→m_remove 非 m_teartape、確認→i_confirm 非 i_confirm_out）、差異一律留痕案例 notes）；d001/d005 加「span 不可得的 v3 列**不塞進 plan**」指示（該列已是獨立 gold：g21/g24）。工具缺口：`--promote` 原擋 ie_modified=true（D3-019「走人工轉正」）——本輪把重切路徑收進同一工作流 | ie_modified=true 走人工轉正（README 後備步驟） vs 收進 `--promote` 資格檢查 | 收進工具（資格檢查唯一出處不分叉）：①`promotion_blockers` 雙向一致性——entry 宣告 ie_modified=true ⟺ 草稿 review_status=ie_edited（編輯過的 plan 以未修改身分轉正＝真 ground truth 被 Plan 層指標錯誤排除，反向＝宣告掛在管線原樣 plan 上）；payload `ie_modified` 唯一出處＝state entry；IE 留在草稿 notes 的涵蓋對應保留、轉正註記附加（管線樣板 notes 才取代）②**逐筆處置**：d008（72dc0511）ie_edited 重切 2 action（a1 acquire g_pick_sel 10.0＋next_operation、a2 CM m_remove@0cm 0.0 記 distance_unstated）→ `--recompile` → **g30**（ie_modified=true）；d001（6fa45cdb）部分重切 2 段（a1 inspect i_confirm 6.0、a2 CM m_press 3.0——v3 GM 放×16＋a_press 416 非 span 字面可得，判型/數量差異留痕）＋切分更正 `ie_ruling: multi_cycle_2` → **g31**（true）；d005（5cb719bb）部分重切 3 段（a1 acquire 10.0、a2 CM m_remove 0.0 distance_unstated、a3 move_place p_place_none 6.0 盤面情境=建議稿 a4 節）＋`ie_ruling: multi_cycle_3`＋zero_tmu/p_direction 面向（F3/a4 為建議稿內 IE 核可結論）→ **g32**（true）；d012（fe5391c6）hint 解讀更正 `single_cycle`（照 d026 前例 ruling_history 留痕，`superseded_source: v3_structure_confirmed` 為守恆對帳標記）、plan 不動 → **g33**（false，仍自我指涉排除）；d030（fe1f3a90）同型更正 single_cycle，**不轉正**——第五輪新判型旗（null→GM）IE 未確認，資格照擋（不越權）；d016（d0350279）新欄 `resegmentation_ruling: "title_sentence_no_resegmentation"`（`ie_ruling` 是 single/multi 硬 regex，語意另立欄位＝D3-019 zero_tmu scope 前例；驗證：唯一合法值＋multi_cycle 前提＋附著切分面向；投影進 ie_review 供 --force 存活）＋fail-closed 擋轉正，notes 記 5 列全由獨立 gold 覆蓋（g27/g26/g18/g22/g15——建議稿當時 rows[0] 尚為草稿，D3-021 已轉正）③**Planner 指標誠實變動（轉正前後各跑一次 eval）**：前＝n27／plan_metrics_n 3／excl 24／acc 0.6667（2/3）／span F1 0.5714（tp2 fp1 fn2＝4/7）；後＝n31／**plan_metrics_n 6**／excl 25／**acc 0.3333**（2/6）／**F1 0.2353**（tp2 fp4 fn9＝4/17）。逐筆變因：g30（gold 2 span [0,11)[12,18) vs planner 整句 [0,18)→match 0、fp1 fn2）、g31（[0,8)[9,40) vs [0,40)→0、fp1 fn2）、g32（[0,5)[6,11)[12,21) vs [0,21)→0、fp1 fn3）——rule planner 恆單 action，多 action gold 必然拿 0，**下降是預期且誠實**（真 ground truth 進分母）；g33 ie_modified=false 仍排除（橡皮圖章回歸仍綠）。釘值文件化：PROMOTED_GOLD_N 24→28、新增 IE_MODIFIED_GOLD_N=3、GOLD_TOTAL_N=31、Plan 層新基線 2/6＋4/17（`_seed_gold_baseline.py`＋`test_planner_eval.py`）；切分確認守恆改三項對帳 10（草稿）＋25（promoted）＋4（superseded→ie_ruling）＝39。**六輪對比**：詞典 0→11→15→16→22→22；正式 gold 3→3→3→24→27→**31**（IE 核准 **28/50**）；草稿集 complete 帶 TMU 0→0→27→31→16→13（轉正移出 3）；被擋 53 筆（multi_cycle 未重切類別 6→2：d030 卡判型旗、d016 IE 裁決不轉正）。不變式驗證：seed 3＋既轉正 24 筆 byte-identical（git 零 diff）；compile 31/31 綠。**未裁不動**：X/I 判型票、TMU=0.0 解讀（9+1 筆）、6 筆新判型旗（含 fe1f3a90）。mutation 4 條紅→綠：g30 ie_modified 改 false→基線釘值＋repo 名單雙紅（「錯誤排除」抓得到）／reseg fail-closed 擋拆除→單元＋repo d016 雙紅（「d016 被錯誤轉正」抓得到）／d0350279 偽標 promoted_to→d016 守門＋promoted 雙向比對紅／edited-未宣告守門拆除→`test_edited_draft_without_entry_declaration_blocks` 紅。unit 760／integration 449＋1 skip／golden 全綠 |
| D3-026 | 2026-08-17 | P0 gold | IE 第八輪答案（2026-08-17/18 兩輪討論定案；**任務單編號 D3-025:E——worklog 該號已被 D3-024 複審硬化佔用，本輪依序記 D3-026**）：D3-024 快答題「X 承載做工時 M 可為零」IE 裁**「9 筆不是同一種狀況」，分五型處置**——①E 純檢查（2f0cb396「並確認DIMM點位」）＝**完整性窄豁免**：純 I 句（面命中集合恰 {I}、無 X/M/工具動詞）M=0 算完整、帶真 I 工時（IE 原話「目視確認無手部移動」）②A 重複鎖附 N 明述（ee5c168e x4／bc473698 x6）＝`distance_unstated` 誠實轉正等佈局 ③C 單次鎖附（323b04c1）同 A ④G 清潔（51077fd1）同 A 另帶 x_seconds_required（秒數也未述）⑤D「並…」接續（3791550c／7ff8b879）同 A、接續配對查 v3 前列 ⑥B 多顆 N 未述（130bb1ad）＝`count_unstated` 與 distance_unstated 並存（IE 裁：留著等佈局的螺絲群屬性）⑦F 面切斷（b49a90ee）＝解析修復開票、案例本身 distance_unstated 誠實轉正 | E 豁免全面放寬 (a)（X 也豁免）vs 窄豁免——**IE 否決 (a)**：「純製程」（C/G 型鎖附/清潔）被剔除出豁免的原因＝**其移動真實存在**（鎖附有起子行程、清潔有風槍掃動），只是距離/秒數句面未述——那是資訊缺漏（誠實 incomplete）不是 M 恆為零；複合 reason 形狀（list vs 「+」串接字串）——選字串（既有 expected_incomplete_reason 是 str、canonical 順序唯一寫法可讀且決定性）；G 面是否可伴隨豁免——查證「並確認DIMM點位」實句面集合恰 {I}（無 G 面），且 F 型 d001（接觸+確認＝G+I）IE 裁誠實 incomplete＝**G 面不豁免**（手部已介入非純目視） | **E 豁免實作**（改動最小化——只動 compile 完整性判定＋判準輸入）：`most_compiler/compile.py` `compile_plan` 新增 keyword `face_hit_params`（None＝不豁免 fail-closed），CM 分支豁免條件＝`missing_core_m`＋action_type=controlled_move＋面集合恰 `{"I"}`＋i_code 已掛＋m_comps 空（句面距離也擋）；判準輸入＝`nlp/linking.py` **`SlotLinker.face_hit_params`**（候選層看不到未查詢參數面——controlled_move 不掛 G spec，G 面命中須獨立證據；掃描文本＝`_query_text` 同一 fallback、完整混合詞典同 `used_lexicon_entries` 視角、只認 slot 參數面）；四個生產呼叫端全數傳入（run_pipeline／gold_eval／wi_ai_service／--recompile）。修後 d021＝complete 6.0（A0 B0 G0 M0 X0 I6 A0）routing=review（no_candidate_m＋i_range_assumed 在）。**生產 nl-draft 行為變更**：純確認句（I 面命中且無其他 slot 面的 controlled_move）從 incomplete missing_core_m → complete＋真 I TMU（routing 仍 review 不 auto）；其餘句型零變化（語料查證：56 草稿中面集合恰 {I} 者恰 d021 一筆）。**8 筆誠實轉正機制**：review-state 新面向 `incomplete_ruling/_ruled_by/_ruled_date`（token=`distance_unstated`/`count_unstated`/`x_seconds_required` 依 tuple 序「+」串接＝canonical 唯一寫法；`_validate_incomplete_aspect` 硬驗；**與 zero_tmu 前提互斥**（complete+0.0 vs 有 incomplete cycle）load 即擋；stale 基準＝草稿已無 incomplete cycle（`incomplete_premise_absent`）；合併寫 `expected_incomplete_reason`＝D3-018 M1 誠實記錄路徑；投影進 ie_review、退場（superseded_aspects）同批 validator 覆蓋；schema 5h 擴為 zero/incomplete 二擇一同進同出＋各自前提）。**D 型接續配對查證**（唯讀 DB）：d013（3791550c）＝wi-template 50ed054b『拿取電動起子，依圖示鎖附兩顆螺絲』version 9569aeaf rows[2]，**前列 rows[1]＝『右手抓握电动起子保持住至機箱』（g08）＝起子在手**（tool_held_for 語意）——直接配對；d015（7ff8b879）自身出處＝獨立 action module（c7c55293 單列版本）**無前列**，接續證據間接（同文 RH 變體＝50ed054b rows[2]）——兩筆配對關係與誠實邊界記進案例 notes（轉正 payload 保留）。**F 票（記票不實作）**：「推×16至」被數量記號插斷——lexicon 面比對對「動詞+數量記號+方向補語”連寫（推×16至）不穩健，M 面 `推至` 漏配→ d001 只剩 G+I 面；修復方向＝match 前數量記號歸一或詞面容忍中綴，動 `nlp/lexicon.py`/`quantities.py` 需另輪（含判型回歸）。**佈局 Phase B 設計註記（等佈局資料）**：重複鎖附（A/B 型）拆列＋頻率模式＝列1 長移×1（取位/首顆對位）＋列2 短移×(N−1)（顆間短移），`tool_held_for` 表達起子在手（g08 前例）；**列2 頻率取 N−1 vs N（首顆是否已含於列1）＝Phase B 待裁參數**（記 README 快答清單，不預落值）；B 型 count_unstated 屆時由佈局的螺絲群屬性補 N。**第五批轉正 9 筆 g41–g49**（逐筆 compile 預檢綠才落筆；split=test、approved 2026-08-17、ie_modified 全 false）：2f0cb396→g41_confirm_dimm_points（**唯一帶真 TMU 6.0**）、ee5c168e→g42、bc473698→g43、323b04c1→g44、51077fd1→g45（reason=distance_unstated+x_seconds_required）、3791550c→g46、7ff8b879→g47、130bb1ad→g48（reason=distance_unstated+count_unstated）、b49a90ee→g49；累計 **IE 核准 44/50**。第八輪 harvest `--force` ×2（轉正前後）：決定性二跑 byte-identical；轉正後草稿池 56→47（DB 候選池扣除已轉正、無新補位）、applied 27→18＋promoted 44 跳過、stale 0；**seed 3＋既轉正 35 筆 git 零 diff**（9 筆 g41–g49 為純新增）。釘值：PROMOTED_GOLD_N 35→**44**（GOLD_TOTAL_N=47）；COMPLETE_TMU 基線有意識加 `2f0cb396`；切分確認守恆 5+30+4+0→**1+34+4+0**＝39（4 筆 v3_structure_confirmed 轉正：b49a90ee/51077fd1/3791550c/7ff8b879）；批次 16 筆釘值改「恰 5 筆轉正名單恰等」（REPO_BATCH_PROMOTED）；distance-rulings 草稿重編號同步（d004_51518399/d029_7f085e02）。**planner 指標不動＝0.3333/0.2353**（plan_metrics_n=6、excl 32→41——9 筆 ie_modified=false 全數自我指涉排除，橡皮圖章回歸仍綠）、compile **47/47**。CI_GATES 規則 8 照做：E 筆合成句（核心格空＋I 面命中）跑完整 engine_gate（`test_engine_gate_pure_i_exemption_true_tmu`＋G 面反例，規則 8 文字同步四條）。mutation 7 組逐一紅→綠：**豁免放寬 X 也豁免（3 紅：unit＋g46/g47 repo 重放——IE 否決的 (a) 被抓）**／放寬 G 也豁免（3 紅含 g49）／豁免整段拆除（3 紅含 g41）／fail-closed 拆除（1 紅）／incomplete 值驗證拆成鍵名（6 紅）／前提互斥拆除（1 紅）／stale 前提＋apply 寫入拆除（各 1–2 紅）；每輪還原清 `__pycache__` 再驗（D3-023 教訓）。unit **836**（817→836）／integration 449＋1 skip／golden 全綠／`most_engine/` 零改動／migration 零／ruff 綠／mypy 62＝基線零新增 |
| D3-027 | 2026-08-17 | 硬化 | code-reviewer 對 D3-026 的複審。必修 **H1**：E 型豁免的假設不可見——「旋轉旋鈕並確認」「翻轉主板並確認」等**有真實移動**的句子（動詞不在字典）面集合塌成 {I} → 豁免 → complete 6.0，而 `issues.remove("missing_core_m")` 讓「移動被當成不存在」零痕跡；且擋 auto 靠的是「M 候選 chosen=None」的結構巧合（linker 實作細節）非顯式保證。隨批 **M2** gold 端 reason 值不承重（實測 g45 複合 token 改掉／g48 改 `totally_made_up`／g49 issues_contain 清空——836 全綠零測試觸及）；**M3** g49＝F 票已知缺陷的化石、檔內零標記（F 修好那天 g49 轉紅，修票的人沒線索）；**L1** 9 筆轉正 notes 尾句樣板謊言（本批無 TMU=0 裁決卻句句宣稱「TMU=0 裁決見…」）；**L4** e2e fixture 重放少傳 `face_hit_params`（自稱管線重放實走 fail-closed 分支——今天無害（現有 fixture 是 inspect 型），加入純 I controlled_move fixture 那天會靜默釘錯答案） | H1：清空 issues vs 掛假設旗標（審查方案 1）——選旗標並**正式化已知限制**：豁免類別實為「**含 I 面且無其他字典內 slot 面**」（lexicon 視角；vocab 遮蔽讓字典外移動動詞句同樣落入）＝掛 F 票修復條件（F 修好類別縮小、旗標語意不變）；M2：另寫一份 gold 端驗證 vs 抽共用——抽單一出處不抄第二份 | **H1**：`compile_plan` 豁免時不清空 issues——`missing_core_m` 原位換成假設旗標 `m_zero_pure_inspection_assumed`（與 `i_range_assumed` 同風格；complete 照給、真 I TMU 照帶；旗標非 `missing_core_*` 前綴＝天然活過 engine gate 的 strip、進 routing_reasons）；`routing._eligible_auto` blocked 集合＋`ROUTING_REASONS` 釘入旗標（顯式保證——`test_auto_blocked_by_m_zero_pure_inspection_assumed` 以「無 M 候選集」合成形狀證明不靠結構巧合）；g41 期望值同步（expected_cycles issues_contain 含旗標＋notes 記豁免類別限制）；字典外移動動詞代表句（旋轉/翻轉）進測試（`test_out_of_lexicon_motion_verb_flagged_not_silent`：斷言面集合塌成 {I}＋complete＋旗標在＋routing review）。**M2**：`incomplete_ruling_canonical_error` 抽共用（`_validate_incomplete_aspect` 改呼叫它）；`test_gold_promotion` 新 repo 不變量 `_reason_ruling_errors`——reason ⟺ 裁決（zero_tmu/incomplete）同進同出且**相等**、token 跑同一套 canonical 驗證、incomplete 裁決必須對得到 complete=false 且 issues_contain 非空的 expected cycle（g49 型清空＝裁決懸空）。**M3**：g49 notes 明寫 F 票關聯＋修復後指示——M 面命中後 cycle 轉 complete 但 distance_cm 會是**被發明的 0.0**（比誠實 incomplete 更糟），重審必須含距離裁決（維持 distance_unstated 或 IE 補真距離走 --relock-approved）＋×16 頻率重看，不得只把期望改成 complete 了事。**L1**：`promoted_case_payload` 尾句依**實際存在的覆核面向**生成（切分確認/判型確認/P 方向數確認/TMU=0 裁決/incomplete 裁決逐項判存在）；g41–g49 notes 尾句同步修正（既轉正 35＋seed 3 凍結不動）。**L4**：`test_e2e_plan_fixtures` 補傳 `face_hit_params`（與生產四呼叫端同形）。**記票不實作**：**M1** 面集合逐參數池（vocab 遮蔽 latent——`face_hit_params` 只報「哪些參數面有命中」，不報「動詞 token 是否全數被字典吸收」，字典外動詞的存在本身無訊號；正解需 lexicon 對 span 的覆蓋率視角）；**L5** 8 筆誠實 incomplete gold（g42–g49）expected_cycles 補釘 x_code（X 面已掛值但期望未鎖）；**L2** compile 豁免條件的冗餘 guard 與 docstring 措辭；**L3** `i_range_assumed` 由 candidates 層（review_reason）非 issues 進 routing_reasons 的措辭澄清。mutation 逐條紅→綠：旗標拆掉回靜默清空（4 紅：test_compiler＋test_linking pure_i＋代表句×2）／旗標拆出 `_eligible_auto` blocked（1 紅）／canonical 驗證三種壞值 in-memory 逐一（改序/自創 token/issues_contain 清空）＋repo 級 g48 改壞值（1 紅）／notes 樣板改回硬寫（1 紅）／L4 參數抽掉（豁免類 fixture 防護；現有 5 fixture 不受影響＝行為同 D3-024 前）；還原後清 `__pycache__` 再驗（D3-023 教訓）。unit **842**（836→842）／integration 449＋1 skip／golden 全綠／compile **47/47**／`most_engine/` 零改動／migration 零／ruff 綠／mypy 零新增；既轉正 35＋seed 3 零 diff（g41–g49 允許本次補強：g41 期望值＋9 筆 notes） |
| D3-028a | 2026-08-17 | P0 gold | **（準備階段，已由 D3-028 落地）**衝線批覆核材料（P0 44→50 的最後 6+ 筆）：唯讀逐筆跑 `promotion_blockers`（唯一出處）盤點 47 筆草稿資格差距（僅缺切分 5／切分已確認缺實質 12／標題句 fail-closed 1／無 entry 29——後者分單動詞已登記 15、未登記 `?` 動詞 11、無動詞面 3），產出 `docs/llm/gold-review/final-stretch.md`：衝線候選 10 筆快答題（D3-024 點名的 5 筆切分未確認全納；無 entry 池補結構證據最強的 d046（v3 module `1c0e39ae`＋`9569aeaf` rows[0]，g08/g46 同 wi-template 成分列）與 d036（m_btn 前例）；d007/d047/d017/d003 列備位）＋誠實 incomplete 預標（d004 已裁 `distance_unstated`、d029 視距離裁決）＋達標預估（最低組合＝候選 1–5 全過＋6–10 任一；全過 54、否決超過 4 筆則本池衝不到 50、缺額需真實工單） | 候選只挑高把握 6 筆 vs 10 筆留餘裕——選後者（IE 可否決個別筆；達 50 只需 6/10 過關）；預設答案預填 vs 距離不預填——距離維持 distance-rulings 誠實邊界不預填 | 僅產文件與本行 worklog——未動草稿/review-state/DB/src；`pytest tests/unit/test_gold_draft_schema.py -q` 52 綠（未動草稿佐證）；IE 答案落地（批次切分/同義詞登記/ie_edited→--recompile→--promote/釘值更新）另記 |
| D3-028 | 2026-08-17 | P0 gold | IE 第十輪答案（2026-08-17 親答；對 `docs/llm/gold-review/final-stretch.md` 的 10 筆候選）：**Q1 OK**（`1dd7c1d5`／`6be614c5`／`2e7b2e5a` 切分批次確認）／**Q2 OK**（`51518399` 切分批次確認，TMU=0 沿用 D3-019 已裁 `distance_unstated`）／**Q3 維持**（`7f085e02`「撕除螢幕保護膜」——IE 原話**「維持不考慮要撕多遠」**＝維持資訊不足，非補距離）／**Q4 不登記**（`0e83128f`「下壓」、`00104439`「按下」留草稿）／**Q5 OK**（`28f9ed7e`→acquire、`8ef772ab`→acquire、`37fbd2a6`→move_place＋機構件 `p_place_single`）。Q4 IE 反問「動詞應該要按照 most 字典庫去查吧？」→ 本輪最重要的沉澱：**同義詞登記準則**。 | （1）Q4 兩筆登記 vs 不登記——**不登記**：查證 22 條既有登記，21 條的 synonym_norm 與該 option 的`label_zh`/`sentence_text_zh` **有子串關係（雙向）**、唯一例外「插入」有 D3-021 明確裁決；「下壓≈按壓」「按下≈按動按鈕」字典查無＝工程端相似性判斷。同義詞決定 option code、option code 決定 TMU ⇒ 相似性登記＝讓工程判斷改工時，從側門繞過 ADR-014 值權威且無機制可察。查證 v3 認證字典**無 alias 欄位**（欄位名逐一查證），(a) 只能靠標籤/句面文字。（2）子串比對單向 vs 雙向——**雙向**：單向（詞⊆標籤）會誤殺 10 條既有登記（`放`⊆`放至`、`組`⊆`組至`、`推`⊆`推至`……字典句面用單字動詞、工單寫帶方向補語的複合詞）。（3）例外機制放 DB 欄位 vs repo allowlist——**repo allowlist**（任務指示；不新增欄位＝零 migration，且例外進版控/被 review/可 grep，比照 `SEED_GOLD_IDS` 白名單）。（4）`acquire_without_place` 的解法：把它從 blocker 名單拿掉（降級為純 WARN）vs 補答案載體——**補載體**：README 轉正資格明列它是 blocker，降級等於讓「取而無放」不再有人看。（5）新面向要不要綁stale 前提（比照 zero_tmu/incomplete）——**不綁**：裁決是句子的性質、不寫任何期望值，旗標不在時惰性不可能讓值變錯；綁了反而讓唯一的合法順序（先答 → 改 plan → recompile 旗標才出現）走不通。 | **同義詞守門**（新檔 `tests/integration/test_synonym_registration_governance.py`，8 測）：準則＝「(a) 詞出現在對應選項自己的標籤/句面文字裡（正規化後子串，任一方向）或 (b) 有明確 IE 裁決；**工程端不得以相似性自行判定**」；對 DB 現存**每條** `rule_option_synonyms` 斷言，(b) 走 `IE_RULING_ALLOWLIST`（鍵 `(parameter, synonym_norm, option_code)`、值＝裁決引用；現況恰 1 條＝`(P,插入,p_asm_single)`→D3-021）＋兩條衛生檢查（allowlist 無 stale 條目、每條指得出 D3-NNN）；`parameter='A'`／`vocab` 無可定址選項 → fail-closed 判未授權（`create_synonym` 對 A 跳過 FK 驗證，該路徑真的寫得進 DB）。**誠實邊界寫進 ADR 與測試 docstring**：這是 **provenance** 測試不是語意正確性測試——單字標籤（`放`/`組`/`推`）的子串判定本來就寬（`放棄` 也會過），它擋的是「字典完全沒有這個詞素」的相似性判斷；要收緊得等字典長出 alias 欄位。**ADR-023 §3.3 新增「規則 1 補節二」**（接續 v2_0038 那節）：準則全文＋22 條現況分類表（21 標籤衍生／1 IE 裁決）＋雙向子串的實例＋為何字典給不了 alias＋違反後果＋守門與 mutation＋未登記兩筆的解鎖路徑（IE 裁決 或 改 JSON 重跑 converter）。**`acquire_lint_ruling` 新面向**（`scripts/gold_harvest.py`）：唯一合法值 `place_in_next_row`（fail-closed；`_validate_acquire_lint_aspect` 硬驗）＋`_ACQUIRE_LINT_ENTRY_KEYS` 面向偵測＋投影進 `_REVIEW_BLOCK_KEYS`＋`caveat_resolution_blockers` 新分支（無裁決照舊擋）＋覆核表狀態行/stale banner/轉正 notes 面向清單同步；順手把四處字面 `"acquire_without_place"` 收斂成 `ACQUIRE_LINT_CAVEAT` 常數（改名漏改即靜默失效）。**第六批轉正 8 筆 g50–g57**（split=test、approved 2026-08-17、IEC141289）：`1dd7c1d5`→g50／`6be614c5`→g51／`2e7b2e5a`→g52（各帶真 TMU **3.0**，`A0 B0 G0 M3 X0 I0 A0`）、`51518399`→g53／`7f085e02`→g54（**誠實 incomplete** `distance_unstated`）、`28f9ed7e`→g55（10.0）／`37fbd2a6`→g56（16.0，`A0 B0 G0 A0 B0 P16 A0`）／`8ef772ab`→g57（10.0），後三筆 `ie_modified: true`（edit 只改 `action_type`／`unresolved`，span 與 action 數不動 → `--recompile` → entry 宣告 → promote）。累計 **IE 核准 52/50——spec §19 P0 的 50 筆門檻已跨過**（6 筆帶真 TMU＋2 筆誠實 incomplete，引用要分開講）。**釘值**：`PROMOTED_GOLD_N` 44→**52**、`IE_MODIFIED_GOLD_N` 3→**6**（`GOLD_TOTAL_N`=55）；`PLAN_ACCURACY` 2/6→**5/9**、`BOUNDARY_MICRO` (2,4,9)→**(5,4,9)**、`BOUNDARY_F1` 4/17→**10/23**（plan_metrics_n 6→9、excl 41→46）；批次確認釘值 16→**22** 且 `REPO_BATCH_PROMOTED` 5→**13** 條；`IE_MODIFIED_GOLD_IDS` 拆成 **reseg 族**（g30–g32，多 action → planner 必然 0）與 **typing 族**（g55–g57，單 action、span 未動 → 必然 1）——兩族預期相反，合成一句「全部拿 0」會整片說謊。**指標上移的誠實邊界**（逐筆變因）：g55/g56/g57 各 1/1 action 命中、span `[0,7)`/`[0,12)`/`[0,11)` 完全相符 → accuracy +3、boundary tp +3；但 **IE 改的是 `action_type`，span 與 action 數維持 planner 輸出**，那 3 分 boundary 是「planner 自己的 span 經 IE 覆核未改」，證據力弱於 D3-022 重切族。橡皮圖章回歸仍綠（未修改者照樣排除）。**距離裁決表結案**：`distance-rulings.md` C11（唯一未裁筆）標「已裁：維持」，待裁筆數 **11→0**（內容全為維持資訊不足；A 段 9 筆改判仍開放走 `--relock-approved`）。**不變式驗證**：seed 3＋既轉正 44 筆 `git diff tests/gold/wi_plans/` **零 diff**（8 筆為純新增）；harvest `--force` ×3（轉正前 1＋轉正後 2）——轉正前草稿池 47 筆與轉正前快照 byte-identical（僅 7 筆多出 ie_review 投影）、轉正後二連跑 byte-identical（含覆核表/摘要）；compile 全綠才落筆（8/8 預檢過）。**mutation 逐條紅→綠**：①acquire_lint 分支恆放行 →`test_unresolved_lint_flag_blocks` 紅 ②分支整段刪除（回 else fail-closed，答了也沒用）→3 紅（含 repo 級 `test_repo_promoted_gold_passes_promotion_invariants`——g55/g57 帶該旗標）③token 驗證拆掉（自創值放行）→`test_acquire_lint_ruling_resolves_flag` 紅 ④`label_derived` 恆真 →5 紅 ⑤`unauthorized_synonyms` 略過標籤衍生檢查 →3 紅 ⑥allowlist 清空 →3 紅（含主守門）；另 `test_gate_catches_a_similarity_synonym_written_to_db` 把假同義詞**真的寫進 DB**（savepoint 隔離、teardown rollback，實測 22 條零殘留）證明守門讀的是 DB 現況不是死名單；每輪還原後清 `__pycache__` 再驗。**驗收**：unit **843**（842→843）／integration **457**＋1 skip（449→457，新檔 8 測；無 DATABASE_URL 時 **7** skip、1 純函式測仍跑（**D3-030 L3 訂正**：原記 6，實測 7——該檔 8 測中 7 條需 DB））／golden 全綠（84＋26＋57）／`wi_ai_eval` compile **55/55** 退出碼 0／ruff `scripts/ tests/` 綠／`most_engine/` 零改動／`src/` 生產碼零改動／**migration 零**（不新增 DB 欄位） |
| D3-029 | 2026-08-17 | P0 gold | **IE 否決 D3-028 的單一 token 設計**（2026-08-17 親答）：`acquire_lint_ruling` 上輪只定義 `place_in_next_row`（「『放』在下一句/下一列」）——IE 原話「**不一定都會是下一步才有放，要看是什麼物件也要看是什麼動作**」，改裁**四類歸宿**：`placed`（放置到位——同列 P 格或後續列的放置動作）／`consumed_by_later_action`（無獨立放，物件被後續動作消耗/固定：螺絲→被鎖附、膠帶→被貼附）／`tool_held`（工具跨列持有本來就不放：起子→保持住）／`genuinely_missing`（建模錯誤——取了之後物件消失，**這才是這條檢查真正要抓的**）。IE 不變式「取最後一定有放，不然會出錯」**依然成立**：「放」有三種合法形態，lint 要抓的是第四類。**教訓（本輪最重要的沉澱）**：上輪的單一 token 是**工程推定**——取自 Q5 預設答案的字面「單純取料、放在後續列」，IE 對 Q5 答 OK 但**沒有被單獨問過歸宿分類**；工程端把「IE 沒反對」讀成「IE 裁定只有一種歸宿」，再用 fail-closed 的口吻把推定寫成唯一合法值（`_validate_acquire_lint_aspect` 的錯誤訊息還寫「目前唯一合法值是…」）。**fail-closed 保護的是「未定義的值不得登記」，保護不了「定義本身是猜的」**——值域是領域知識，唯一出處是 IE，不是從單一案例外推。實據：v3 module『拿取電動起子，依圖示鎖附兩顆螺絲』（wi-template `50ed054b`／version `9569aeaf`）三列＝rows[0] 左手從螺絲料盒拿取螺絲(CM)／rows[1] 右手抓握電動起子保持住至機箱(GM，`p_hold`)／rows[2] 右手鎖附固定並確認螺絲到位(CM)——兩手各取一物、一起到目標執行：螺絲的放被鎖附消耗、起子根本不放。這份證據 D3-026 就查過（D 型接續配對「起子在手」），上輪卻沒拿它反證自己的 token。 | （1）`genuinely_missing` 要不要解除轉正阻擋——**不解除**：它是「這筆建模錯了」的宣告不是通過條件，解法是補收尾 action 走 `--recompile`（旗標因 plan 改變自然消失）；放行等於把 IE 已判定為錯的建模寫進標準答案。（2）`place_in_next_row` 保留為別名（≈`placed`）vs 撤回——**撤回**：留著等於讓被否決的推定繼續放行；改列 `ACQUIRE_LINT_RETRACTED_RULINGS`，**只在 `ruling_history` 的更正軌跡裡合法**（原值全文要驗得出來不是 garbage，但不得再當現行裁決）。（3）`tool_held` 自動推導的位置：旗標端靜音 vs 裁決端推導——**裁決端**：旗標是 plan 的函數且與 schema 守門（5c）同進同出，靜音會讓覆核表看不到未閉合的取；裁決端推導＝IE 仍看得見、只是不必逐筆答。（4）自動推導認哪些 dependency——**只認 `tool_held_for`**：`same_object` 可以是放好後再對同一物件作業、`uses_tool` 只說用到工具，都不主張物件停在手上，拿它們推導＝自動幫 IE 答錯。（5）`ruling_history` 的改判怎麼記——**新增第三型 `superseded_rulings`**（面向還在、只有值被改判），不變量與 `superseded_aspects`（退場）**相反**：退場要求本體沒有那些鍵，改判要求本體有那些鍵且值不同。 | **四類實作**（`scripts/gold_harvest.py`，`src/` 與 migration 零改動）：`ACQUIRE_LINT_RULING_TOKENS` 四值＋`ACQUIRE_LINT_CLEARING_RULINGS` **三值放行集合**＋`ACQUIRE_LINT_RETRACTED_RULINGS`＋`ACQUIRE_LINT_RULING_ZH`（四類中文說明單一出處，覆核表/摘要/旗標說明共用）；`_validate_acquire_lint_aspect` 改列舉驗證（fail-closed，`allow_retracted` 只給軌跡用）；轉正資格抽成 `acquire_lint_blockers`（`genuinely_missing` 走**不放行**分支、未知 token fail-closed）。**tool_held ⇄ tool_held_for 查證結論**（兩者＝同一事實的兩層：dependency 是契約層、會讓 compile 端 `most_compiler/policies.py::is_tool_held` 把後續 action 的 G 留空＝**改 TMU**；裁決是覆核層、只解除阻擋）：①**能自動推就自動推**——`acquire_lint_auto_ruling`：每個未閉合 acquire 都有 `tool_held_for` 指向同 plan 更後面的 action ⇒ 裁決自動推得 `tool_held`，覆核表印「自動推得、不需回答」，轉正端免 IE 逐筆答（正式 gold g02「拿取電動起子…鎖附兩顆螺絲」即此型）②**互相驗證**——`acquire_lint_tool_held_unexpressed`：IE 裁 `tool_held` 但同 plan 內**可表達卻沒表達** dependency ⇒ 擋（`is_tool_held` 不生效，後續 G 會被當成重新抓取而多算 TMU）③**邊界**：acquire 是 plan 最後一個 action＝跨列持有（g08 型），dependency 是 plan 內的、跨列表達不了，此時不擋也推不得，仍由 IE 答。**g55/g57 改判**：ie_review／review-state 兩邊 `acquire_lint_ruling: place_in_next_row → consumed_by_later_action`（IEC141289，2026-08-17），notes 補 v3 三列結構證據（左手螺絲／右手起子 `p_hold`／鎖附消耗）；**推定與撤回留痕**＝entry 的 `ruling_history` 新型條目（`superseded_rulings` 帶被撤回的原值全文＋`supersede_reason` 寫明「工程推定、IE 以領域知識否決」）。**P0 維持 52/50**（兩筆不撤回，只改裁決內容）。**mutation 9 組逐一紅→綠**：①`genuinely_missing` 加進放行集合 ②自動推導拆掉 ③反向驗證拆掉 ④自動推導/反向驗證放寬到 `same_object`/`uses_tool`（兩處同時）⑤token 列舉驗證拆掉 ⑥已撤回 token 在本體放行 ⑦假改判檢查拆掉 ⑧四類名單漂移（拿掉一類）⑨覆核表提問只列三類；每輪還原後清 `__pycache__` 再驗（D3-023 教訓）。**驗收**：unit **871**（843→871，新增 28 測：四類各一條放行/阻擋、自動推導、反向驗證、跨列邊界、改判軌跡）／integration **457**＋1 skip（不變）／golden 全綠（84＋26＋57）／`wi_ai_eval` compile **55/55**、planner 0.5556/0.4348（n=9，不變）／ruff `scripts/ tests/` 綠／harvest `--force` ×2 byte-identical、草稿池 **39** 筆與改動前 byte-identical（**D3-030 L2 訂正**：原記 41，實數 39——`tests/gold/wi_plans_draft/` 扣掉 review-state.json）／seed 3＋既轉正 50 筆零 diff（僅 g55/g57 依裁決改欄）／`most_engine/` 與 `src/` 零改動／**migration 零** |
| D3-030 | 2026-08-17 | 硬化 | code-reviewer 對 D3-028/029 的複審。**B1（阻擋）同義詞守門在 CI 上必紅**：CI 的 seed 鏈（`alembic upgrade head` → `dev_seed_v2.py` → `dev_seed_templates.py`）**沒有任何一步寫入 `rule_option_synonyms`**——22 條詞典只活在開發機的執行期資料裡，守門測試（`assert rows`）在乾淨 DB 上必紅（複審在拋棄式 DB 實測 3 failed）＝**守門在 CI 上從未真的守過**；**B2（高）8 筆裁決日寫成 2026-08-18**（比系統時鐘晚一天、比當批評測報告時戳 `wi-gold-20260817T131338Z` 晚一天——是算錯不是時區）；**M1（中）「假改判」判準漏一種**：`superseded_rulings` 的判準是「不是每一欄都相同」，同一 token 只改日期即被當成改判收下（複審實測 ACCEPTED）；**M2** ADR-023「機械守門」讀起來像全覆蓋；**L1** 守門邊界的例子（`放棄` 也會過）避重就輕；**L2/L3** 草稿池筆數 41／無 DB skip 數 6 記錯 | B1：seed 腳本 vs 測試自建資料 vs migration 資料遷移——選 seed 腳本（詞典是**環境資料**不是 schema：測試自建等於守門對象變成測試自己造的資料、失去「守 DB 現況」的語意；migration 塞資料會讓值權威與治理路徑分家）。B2：日期比對用寫死常數 vs 執行當下——選執行當下（寫死的「今天」跨過那天就靜默失效，且變成另一個要維護的釘值）。M1：判準改「任一欄不同」→「裁決欄（`*_ruling`）必須不同」——輔助欄（`_ruled_by`／`_ruled_date`）的差異不構成改判 | **B1**：新增 `scripts/dev_seed_synonyms.py`（22 條逐條標 D3-016／017／019／021 的 IE 裁決出處與答案內容、`created_by=IEC141289` 沿用原登記人；走 `synonym_service.create_synonym` 與 API 同一條路——FK／retired／同面撞 priority 三道守門都在裡面，不另開平行寫入路徑；**冪等**＝以 UNIQUE 鍵 `(rule_set_id, parameter, synonym_norm, option_code)` 先查證，已存在跳過且不覆寫既有列）＋CI `backend` job 在 `dev_seed_templates.py` 之後補一步。**乾淨拋棄式 DB 實證**（建庫 → `alembic upgrade head` → 三支 seed → 守門）：補 seed 前 **3 failed／5 passed**（重現複審結論）、補 seed 後 **8 passed**；seed 重跑兩次皆「新增 0、略過 22」（無重複列）；22 條與開發機現存 22 條逐欄 `diff` 為空。**B2**：`review-state.json`（26 處，含 `ruling_history` 巢狀日期與檔頭 note 敘述）＋`g50–g57`（22 處）＋4 個單元測試 fixture／釘值＋7 份文件（ADR-023／worklog／README／distance-rulings／final-stretch／synonym-candidates／harvest 註解）全批 **2026-08-18 → 2026-08-17**（81 處），`REPO_BATCH_CONFIRMED_DATES` 收斂成單值；**既轉正 55 筆除日期欄外零改動**。防復發＝`date_shape_error()`（**單一出處**：YYYY-MM-DD ＋ 合法日期 ＋ **不得晚於今天**，基準取 `date.today()`）接上 `_require_by_date`／`segmentation_confirmed_date`／`promoted_date`／`superseded_date`／`--approved-date` 五處。**M1**：`_ruling_value_keys()`（`*_ruling` 命名慣例的單一出處）＋判準改「裁決欄必須不同」，訊息點名「只改日期/登記人不是改判」。**M2**：ADR-023 補節二加「機械守門的範圍聲明」——覆蓋的是版控種子詞典（即本輪那支腳本），**不涵蓋執行期 `POST /synonyms` 的即時登記**（生產環境不跑測試）；準則下沉到 `create_synonym` API 本身＝**記票不做**（值域判定要引用 IE 裁決紀錄，紀錄目前住 repo allowlist，API 端取用需另一套載體）。**L1**：邊界說明改成真實風險——同一參數下多個 code 共用同一詞素時（`放至` 對 `p_place_none`／`p_place_single` 的標籤都含「放」）守門**判斷不出選了哪個 code**，而那正是決定 TMU 的那一步（ADR 補節與測試 docstring 同步）。**L2/L3**：草稿池 41→**39**、無 DB skip 6→**7**。**新增票（不實作）**：①`IE_RULING_ALLOWLIST` 的鍵不含 `rule_set_code`——clone 出新版本後裁決自動延用，未聲明是否刻意（延用有其道理：詞出自字典這件事不隨版本改變；但「IE 對 V2 的裁決自動適用於未來任一版本」該是明示決定）；②`g56` 的 P 方向數（`p_place_single` vs `p_place_none`）決定 TMU，卻**無結構化面向可走 stale 偵測**（該筆 harvest 當時判型未定＝旗標不生成，方向數依據只記在 notes；改詞典或改情境規則時沒有任何機制會提醒重看）。**mutation 逐條紅→綠**：①CI 拿掉 seed 步驟（等價於乾淨庫不跑）→守門 3 紅 ②seed 冪等查證拆除（不 pre-check 直接 create）→第二次跑撞 UNIQUE／`SynonymConflict` ③假改判判準退回「不是每一欄都相同」→假改判 3／4 紅 ④`date_shape_error` 拆掉未來檢查→未來日期 3 測＋repo 自洽測紅 ⑤`_ruling_value_keys` 改成回傳全部鍵→假改判 3／4 綠不了（等價於 ③）。**驗收**：unit **884**（871→884，新增 13 測：seed 表格守門 5、未來日期 6、假改判 2）／integration **460**＋1 skip（457→460，新檔 `test_seed_synonyms.py` 3 測）／golden 全綠／`wi_ai_eval` compile **55/55**／ruff `scripts/ tests/` 綠／`src/`・`most_engine/`・migration **零改動** |

## 5. Checkpoint 紀錄

| 日期 | Phase | 席位/審查者 | 發現 | 處置 |
|------|-------|-------------|------|------|
| 2026-08-07 | L0 | code-reviewer agent | P1：`error` 欄誤用；P2：缺 UNIQUE／abstain／active bundle／legacy 漂移／§7.5 | 全部必修項已修；**APPROVE_WITH_NITS**（optional 測試檔可後補）。資安延後。 |
| 2026-08-07 | L1 | code-reviewer agent | P1：malformed envelope／sanitize 硬失敗／cache provenance／缺 unreachable test | 全修；**APPROVE**。資安延後。 |
| 2026-08-07 | L2 | code-reviewer agent | P1：L0 stub／auto 忽略 quantity_policy／缺 engine_gate 覆蓋 | 全修；**APPROVE_WITH_NITS**。資安延後。 |
| 2026-08-07 | L3-2 | code-reviewer agent | P0 stale 被 setResponse 清掉；P1 前端假 A6 檔位 | 全修；**APPROVE**。 |
| 2026-08-10 | L3 | coordinator | L3-1～L3-3 實作出口彙整 | **impl checkpoint**：自動化證據齊；§0.1 現場 demo／Playwright／資安仍 open。 |
| 2026-08-10 | L4 | code-reviewer agent | P1：bundle pin 未用於 tick／計數非原子／缺 lease reclaim | 全修；**APPROVE**（[L4 review](77df24e2-19a1-47cb-836a-6b1c174d2f79)）。資安延後。 |
| 2026-08-11 | R1 | code-reviewer agent | P0 AI cache 未含 revision；P1 Import/from-module 無 CAS／legacy bypass／Integer vs BigInt | 全修；**APPROVE_WITH_NITS**（[R1 review](ca934a12-62e5-4670-a022-29c7602c86a7)）。資安延後。 |
| 2026-08-11 | R2a | code-reviewer agent | P2：ORM index／fail-closed 測試／CreateOut nullable | ORM index 已補；**APPROVE_WITH_NITS**（[R2a review](52d1b490-4ada-41e8-815d-691c7fd124ed)）。 |
| 2026-08-11 | R2b | code-reviewer agent | P2：policy mismatch／舊 rev 測試缺口；issues JSON string | **APPROVE_WITH_NITS**（[R2b review](9b4a4c82-95cd-4bc1-a864-e1f74a03010a)）。 |
| 2026-08-11 | R3b | code-reviewer agent | P1：缺 UNIQUE(aggregate,event_no)；P2 測試薄 | UNIQUE 以 v2_0033 補；**APPROVE_WITH_NITS**（[R3b review](19d777d6-6a9a-49d5-bfc9-5b19dc078d20)）。 |
| 2026-08-11 | R3a | code-reviewer agent | P1：缺 publish freeze IT；P2 ORM index／standalone revision | freeze IT＋index 已補；**APPROVE_WITH_NITS**（[R3a review](3325fa79-8b6a-4f87-9e0b-0b7386f6a27c)）。 |
| 2026-08-15 | L4 硬化 | checkpoint 三席（security＋code-reviewer×2） | Blocker：F1 毒 job 卡死 loop 且重啟不復原；#1 `_mark_job_failed` 暫時性失敗判死＋狀態機旁路；#2/F6 worker 先死→shutdown 炸/dispose 跳過/死亡靜默。必修：F3 idempotency 未範圍化、F5 `str(exc)` 落 DB、#6 cleanup 漏 search_documents、F2 上傳/配額無上限、F4 撤權不中止、#3 tick 鎖窗 | 全部修復（D3-009～D3-012；migration v2_0037；ADR-030 proposed）。已知缺口：F4 完整物件級授權（D3-011）；LLM 移出交易（D3-012→ADR-030 觸發條件） |
| 2026-08-16 | P0 gold 預標註管線 | code-reviewer agent | P0-1 自我指涉評測（草稿 plan＝planner 輸出，橡皮圖章轉正把指標吹到 0.98）；P0-2 pending_ie 守門只查 action 數（改 action_type/整句換掉都綠）；P1-3「IE 核准」無實質門檻；P1-4 `--recompile` 可零摩擦改寫正式 gold；P1-5 取放配對誤標 multi_action 且 caveat 方向反了（13/42 筆）；P2-6～P2-9 取樣/啟發式/全形/報告污染誠實性；P3 十項小修 | 全部修復（D3-013）。驗證：橡皮圖章實驗排除 60 筆、Plan 層指標釘在基線 2/3 與 4/7 不動；5 條 mutation 逐一紅→綠；unit 583／integration 445＋1 skip／golden 全綠；正式 gold 3 筆 byte-identical |
| 2026-08-16 | D3-017 複審 | code-reviewer agent | 必修：H1 同面撞 priority 無守門（UNIQUE 放寬拆掉治理防線，parser 靜默擇一無旗標）；M1 TMU=0.0 穿透空殼守門＋覆核表無逐筆警語；M2 CI_GATES:84／impl-spec:79 兩處不實敘述；M6 linking docstring 生產端為假；ADR-023 需補節（裁定：不開新 ADR）。其餘：M3 幽靈命中守衛上移生產、M4 棄權安全網措辭、M5 unclassified 進 routing_reasons、M7 list 排序變體相鄰、L1 top_k 擠位、L2–L4 小修 | H1/M1/M2/M6＋ADR-023 §3.3 規則 1 補節已修（D3-018）；M3/M4/M5/M7/L1/L2–L4 記票 D3-018 不實作（M3 明定為「壓合」登記動作的前置條件）。mutation 4 條紅→綠；unit 683／integration 449＋1 skip／golden 全綠；41 筆覆核狀態存活、決定性、正式 gold 不動、DB 15 條 |
| 2026-08-17 | D3-024 複審 | code-reviewer agent | 必修：**H1** seconds 模式 X 掛 chosen → 引擎硬拒 `X_SECONDS_REQUIRED` → 合法草稿 invalid（「按壓把手並清潔卡槽」實測；語料對 complete=True 才觸發的失效結構性失明）；**H2** I 格掛值繞過 `i_range_assumed`（守門綁死 action_type=="inspect"，「按壓DIMM壓合治具的把手並確認到位」實測 auto）；**M1** superseded_aspects 驗證只到鍵名（garbage 日期／非法 seq／半套面向／假退場全 ACCEPTED）；**M2** D3-024 變更清單漏列四項；CI_GATES 缺掛值合成句閘門。其餘：L1 清潔守門脈絡輸入不同源、L2 review_reason 被既有 reason 吞（同 D3-018 M5 類）、L3 X pool 重掃違反單次掃描 | H1/H2/M1/M2＋CI_GATES 規則 8 已修（D3-025）；L1–L3 記票不實作。mutation 4 組紅→綠；unit 817／integration 449＋1 skip／golden 全綠／compile 38/38；harvest 決定性且與 repo 草稿 byte-identical；正式 gold 38 筆零 diff |
| 2026-08-17 | D3-026 複審 | code-reviewer agent | 必修：**H1** E 型豁免的假設不可見——字典外移動動詞句（旋轉/翻轉…並確認）面集合塌成 {I} → 豁免 complete 6.0，`issues.remove` 讓「移動被當成不存在」零痕跡；擋 auto 靠 M 候選空的結構巧合。隨批：**M2** gold 端 reason 值不承重（三種壞值 836 全綠）、**M3** g49 化石零標記、**L1** notes 樣板謊言、**L4** e2e fixture 少傳 face_hit_params。其餘：M1 面集合逐參數池、L5 x_code 補釘、L2 冗餘 guard 措辭、L3 i_range_assumed 來源措辭 | H1＋M2/M3/L1/L4 已修（D3-027：假設旗標 `m_zero_pure_inspection_assumed`＋routing 顯式擋 auto＋reason↔裁決 repo 不變量＋g49 F 票標記＋notes 依實際面向生成；「含 I 面且無其他**字典內** slot 面」正式化為已知限制掛 F 票）；M1/L5/L2/L3 記票不實作。mutation 逐條紅→綠；unit 842／integration 449＋1 skip／golden 全綠／compile 47/47；既轉正 35＋seed 3 零 diff（g41–g49 本次補強） |
| 2026-08-17 | D3-028/029 複審 | code-reviewer agent | **B1（阻擋）**同義詞守門測試在乾淨 DB 上必紅——CI seed 鏈不寫 `rule_option_synonyms`（拋棄式庫實測 3 failed）；**B2（高）**8 筆裁決日 2026-08-18 比系統時鐘晚一天；**M1（中）**「假改判」判準看的是「不是每一欄都相同」，只改日期的假軌跡被收下；隨批 M2（ADR 守門範圍讀起來像全覆蓋）、L1（守門邊界的例子避重就輕）、L2/L3（筆數記錯）；L4/L5 記票 | B1/B2/M1/M2/L1/L2/L3 全修（D3-030：`scripts/dev_seed_synonyms.py`＋CI 補一步、81 處日期訂正＋`date_shape_error` 不得晚於今天、判準改看裁決欄、ADR 範圍聲明與誠實邊界）；L4（allowlist 鍵不含 rule_set_code）、L5（g56 P 方向無結構化面向可 stale）記票不實作。乾淨拋棄式 DB 實證補 seed 前 3 failed → 後 8 passed；mutation 逐條紅→綠 |
| 2026-08-22 | plan-v1.3 prompt 迭代 | checkpoint 三席（code-reviewer＋security-reviewer＋test-engineer） | **P1-1** legacy `provenance.slots_parser` 在快取重播路徑上說謊——重播的 slots 是由 `SlotLinker.link(stored plan)` 重建（planner 是 LLM 時與 rule parser 無關），新欄位卻對兩條路徑都宣稱 `rule_based_v1`，**新欄位犯的正是它要治的病**，且新測試把假宣稱釘成斷言；**P1-2** 重播的 `prompt_version` 報「當下的模組常數」（`ai_parse_runs` 無此欄），本批把常數 v1→v1.3 使 DB 內既有每一筆 LLM run 開始謊報（probe 實證 written `plan-v1.3` → replayed `plan-v9.9-NEVER-RAN`）；**測試汙染** 新 integration 測試漏 `finally: get_settings.cache_clear()`，probe 實證後續測試**真的對 127.0.0.1:11434 發出 HTTP**（本機 ollama 回 404 被 fallback 靜默接住）；**守衛可繞過** 切分守衛的 `_link_between` 三個訊號可被「move_place 省略 object」同時規避（正是 fp 15→36 的形狀），實測補一則正確示範後壞示範完全隱形；**零 unit 覆蓋** `legacy_provenance()` 只有整檔 skip 的 integration 摸得到；**文件四處不實**（規則編號位移、規則 12 立論為假、「補上值域即可」與資料矛盾、g13/g46 歸因錯）；資安 2 High／3 Medium／2 Low **全為既有**，其中 High 2 被本批 few-shot 放大 | P1-1／P1-2／測試汙染／守衛繞過／unit 覆蓋／文件四處**全修**（見 §9 與 spec §7.2、§10.3）。**複審 mutation 逐條紅→綠**：讀常數→3 紅、`slots_parser` 改回常數→1 紅（**unit 層免 DB**）、2c 繞過形狀（含 `status: missing` 變體）→紅（且在「有正確示範共存」條件下仍紅）、stale 豁免→紅、無 DB 環境 `parser` 謊報→5 failed（上一輪同一 mutation 是 1252 全綠）。**結論 APPROVE_WITH_NITS**。驗收：unit **1266**／integration **589**＋1 skip／golden 88·26·60 全綠／ruff 綠／mypy 改動檔零錯。既有資安 High／Medium、P2-5、P2-7 與 6 項 P3 **記票不實作**，理由見 §9 |

| 2026-08-22 | 資安 S-2 修復 | checkpoint 三席（code-reviewer＋security-reviewer＋**ddm-validator**） | **S-2 本體**：`most_compiler` 完全不讀 `RoleValue.status`，無憑據數值直達 TMU 且 distance 路徑無 review 旗標；**NaN fail-open**（資安席）：新守衛的比對式 `abs(measured-value) > tol` 對 NaN 恆 False → 直接放行 → `band_index` 落到 overflow 帶＝**最大 A 檔位 24 TMU、零旗標**；同根因另有 `int(nan)`／`int(inf)` → **HTTP 500**、以及第三個實例 `x_seconds=nan` → **`total_tmu: nan`（非合規 JSON）**；**F3**（validator）：mm↔cm **10 倍**單位混淆零阻力通過——路徑 (1) 已抽到矛盾的真值卻被路徑 (2) 救回去；**路徑 (2) 可被本行詞彙誤觸**（`十字起子` 含「十」→ 捏造 `10cm` 靜默通過）＋**跨 kind 背書**＋**零測試覆蓋**（整段換 `return False` 時 39 支全過）；`十六顆` 被 `_COUNT_RE` 讀成 6 造成**雙向**錯誤；**量級盲單位**（code-review）：`公尺`／`米`／`尺`／`公厘` 在路徑 (2) 表內但抽取器不認得 → 100 倍錯誤靜默通過，且反向會讓正確換算被誤掛旗標；**守衛倚賴外部不變量**（資安席）：掃模型給的 `ev.text` 而非推導切片；**spec 兩處不實敘述**＋**一段「已知弱點」在修好後未同步**（皆為協調者所寫） | 全部修復。**判準演進成三段式**（路徑 1 → 矛盾檢查 → 帶單位退路），非有限值改為**拒收＋留痕**（與主體「照用＋掛旗標」方向相反，理由：非有限值不是『沒出處的量』是『不是量』）。**三席獨立交叉驗證零 TMU 變動**：code-review 的 2688 例合成矩陣（worktree 對 `973e49a` byte-identical）、validator 的 `git archive` 對照樹三組 A/B（gold 55 逐案＋20 探針＋rule planner 全鏈路皆 0 差異）、security 的三條繞過實跑**含正向對照組**（強制 auto flag 確認合法案例真的走到 `auto`）。**mutation 逐條紅→綠**：矛盾檢查拆除→1 紅、退路退回裸數字→2 紅、`finite_or_reject` 變恆等→**9 紅**、中文數字修復拆除→4 紅、旗標移出 blocked→1 紅。**結論**：code-review `APPROVE_WITH_NITS`／validator `PASS`／security 無阻擋項。驗收：unit **1312**（1266→1312，+46 支）／integration **589**＋1 skip／golden 88·26·60（GM=28 CM=29）／eval compile 55/55（gold 三個新旗標出現次數皆 0）／ruff＋mypy 綠。**既有期望 TMU 零變動、無 migration**。S-8／S-11 與 3 個殘留偽陽性記票不實作，理由見 §9 |

| 2026-08-22 | T-1 評測報告執行來源自述（＋S-6） | checkpoint 三席（code-reviewer＋security-reviewer＋test-engineer） | **T-1 本體**：報告不記自己是哪顆模型、哪一版 prompt 跑的，版本歸屬只能靠人工檔名（**與 `973e49a` 修掉的 `provenance.parser` 同一種病**）；**憑證洩漏**（security）：`planner_eval` 把例外原文寫進報告，而 httpx 的 `HTTPStatusError` 訊息內嵌**含 userinfo 的完整 URL**——401/404/429/5xx 都會踩到，且 endpoint 設錯正是最可能重跑並 commit 報告的時候；**requested ≠ served**（code-review）：記的是請求的模型而非伺服器回報的，而 `wi_ai_service` 對同一問題用的是後者＝兩個子系統各有權威；**遠端可控字串**（security，經協調者提問觸發）：`model_served` 無界，且 `print()` 未逸出＝**本 diff 新增的終端機 ANSI 注入 sink**（可覆寫先前印出的評測數字）；**分岐規則零測試**（code-review）：突變成「靜默挑第一個」時 38 支全過；**`llm_timeout_s` 有回報但沒守住它真的被套用**（test）；**兩處 rationale 宣稱過強**（`lru_cache` 下不可達的風險、「rule 不讀 settings」）；**上層 README 的「誠實基線」自己過期**（宣稱 n=3，實際 n=55） | 全部修復。`model` 拆成 **requested／served／variants** 三欄，**分岐不挑代表、全案失敗時 requested 不消失、退回請求值的 caveat 寫進報告自身**；例外訊息一律遮 URL（選通用遮蔽而非特判 `HTTPStatusError`——洩漏的類別是「訊息含 URL」，且只記狀態碼反而會丟掉原因短語）；`model_served` 截斷 200 字元＋剝 C0/C1 並**留痕**，刻意不用字元白名單（誤殺真值＝摧毀該欄位的目的）。sanitize 放在 `observe_served()` 而非 `to_dict()`，順帶擋掉 denial-of-attestation（肉眼相同、位元不同的名字會讓每案成為新 variant、把 served 永遠壓成 null）。**mutation 逐條紅→綠**：拆回原式→3 紅、分岐改挑第一個→1 紅（上輪同突變 38 passed 存活）、sanitizer 改恆等→1 紅、adapter timeout 改 999→1 紅。**結論**：code-review `APPROVE`／security 不阻擋／test 兩支皆活。驗收：unit **1320**（1312→1320，+8）／integration **589**＋1 skip／eval compile 55/55／ruff 綠／`docs/llm/eval-reports/` 零 JSON 變動。T-10／T-11 記票不實作 |


## 6. 驗收紀錄（spec §0.1）

| # | 判準 | 演示日期 | 結果 | 備註 |
|---|------|----------|------|------|
| 1 | 多 action 拆解＋top-K＋TMU 來自引擎 | 2026-08-10 | partial | 自動：gold g02＋L2 multi_action（TMU 引擎）；top-K UI 延後 |
| 2 | 「拿起 DIMM」不補步驟、missing、review | 2026-08-10 | pass* | 自動：gold g01 boundary＋routing review（*缺現場 UI 演示） |
| 3 | replace_candidate review event 可查 | 2026-08-10 | pass* | 自動：`test_ai_review_api` synonym candidate（*缺現場 UI） |
| 4 | LLM 關機 fallback 無損核心 | 2026-08-07 | pass | integration unreachable／disabled |
| 5 | 黃金錨點全綠（GM=28、CM=29） | 2026-08-10 | pass | `scripts/core_logic/run_all.py` 全綠 |

## 7. 待決事項狀態（鏡射 spec §19，裁決後更新）

| # | 事項 | 分級 | 狀態 |
|---|------|------|------|
| 1 | quantity 展開 policy | D2（IE 裁決） | open |
| 2 | inspect 併入前 cycle | D2（modeling policy） | open |
| 3 | 手別/SIMO 自動分配 | D2（IE 裁決） | open |
| 4 | 雲端 LLM | **D1（需 ADR）** | open |
| 5 | auto-accept 開啟 | **D1（需 ADR）** | open |
| 6 | `ai_*` 獨立 PostgreSQL schema | **D1（需 ADR）** | open |
| 7 | worksheet revision 綁定 | D2（R1 後加法） | open |

## 8. Planner 量測紀錄（prompt 迭代）

> **量測條件**：`scripts/wi_ai_eval.py --planner llm`、gold n=55（IE 核准 52＋seed 3；
> 自我指涉排除 3 筆，Plan 層 `plan_metrics_n=55`）、**qwen2.5:14b**（`DDM_LLM_MODEL`）、
> `--llm-timeout-s 300`、地端 ollama。
> **報告落 `docs/llm/eval-reports/local-14b/`（獨立子目錄，已入版控）**：上層目錄是
> 設定模型（32B）的正式報告區，14B 是「本機有什麼就先量什麼」的替代品，同名混放會讓
> 讀的人分不出哪份是設定模型跑的。該目錄的 README 說明命名與界線。
> **本表的數字不得拿去對 `docs/architecture/wi-ai-parser-system-spec.md` §14.4
> 的上線門檻**——那條門檻是對設定模型講的。
> 每一版的規則改動理由見實作 spec §7.2（不在本表重複）。

報告檔名皆位於 `docs/llm/eval-reports/local-14b/`：

| 版本 | 報告（`plan-*.json`） | 成功 | action_count_acc | boundary F1 | P / R | 說明 |
|------|------|------|------------------|-------------|-------|------|
| plan-v1（修 offset 前） | `v1.0-pre-offset-repair-20260821T110144Z` | 6/55 | 0.109 | 0.182 | 1.000 / 0.100 | P=1.0 是假象：只吐 6 個 span 剛好全中，分母 6 |
| plan-v1（＋evidence offset 修復） | `v1.0-offset-repair-20260821T180136Z` | 36/55 | 0.545 | 0.558 | 0.659 / 0.483 | **＝ HEAD（b12575d）的水準** |
| plan-v1.1 | `v1.1-timeout-tainted-20260822T023246Z` | 29/55 | 0.418 | 0.484 | 0.657 / 0.383 | 作廢：21 次 ReadTimeout 汙染（timeout 過短），僅留存不作結論 |
| plan-v1.1 | `v1.1-20260822T030309Z` | 45/55 | 0.473 | 0.488 | 0.463 / 0.517 | 放寬 timeout 後重跑。**fp 15→36**＝新增的第 2 則示範教錯切分 |
| plan-v1.2 | `v1.2-20260822T040111Z` | 45/55 | 0.709 | 0.708 | 0.755 / 0.667 | 換掉壞示範＋寫明切分慣例；fp 36→13 |
| plan-v1.3 | `v1.3-run1-20260822T042735Z` | 48/55 | 0.782 | 0.735 | 0.754 / 0.717 | 補 action_ref 值域／禁佔位字／dependency type 白名單 |
| plan-v1.3（重跑） | `v1.3-run2-20260822T061810Z` | 50/55 | 0.800 | 0.733 | 0.733 / 0.733 | **變異確認**：與上一列同版本、同條件 |

**變異（同版本兩輪）**：成功 48 ↔ 50、acc 0.782 ↔ 0.800、F1 0.735 ↔ 0.733。
F1 幾乎不動、成功數 ±2——**v1.3 的水準不是抽樣運氣**，可以當成基準往下走。
（單輪數字不足以宣告改善：v1.1→v1.2 的 fp 36→13 才是超出此變異幅度的真訊號。）

**剩餘失敗的分類**（兩輪交叉比對，這是下一輪該打的靶）：

| 類型 | 案例 | 性質 |
|------|------|------|
| 持續失敗（兩輪皆紅） | `g13`、`g46`、`g47`、`g48` | **同一案兩輪可能敗因不同，別當成單一成因**：`g13` run1 是自創 `same_hand`（`json_or_schema`）、run2 是 `explicit_without_evidence:a1:hand`（`same_hand` 在 run2 出現 0 次）。`g46/g47` 兩輪都是在 a1 用 `inferred` 指向不存在的 action（規則 4 明文禁止第一個 action 用 inferred）；`g46` run2 是 a1＋a2 各一次，不只 a1。**規則寫了但沒被遵守，加規則已達邊際效益遞減** |
| 既有契約問題 | `g48` | `quantity.value` 收到字串「多顆」→ Pydantic float 解析失敗。屬 D2（quantity 展開 policy，§7 待決 #1），非 prompt 可解 |
| 抖動尾巴 | `g24`／`g34`／`g49`（僅第 1 輪）、`g35`（僅第 2 輪） | 約 3 案在成功/失敗邊界擺盪；**不要用單輪結果判斷這幾案是否修好** |

**觀測**：`evidence_offset_repaired` 觸發次數 `out_after` 124、v1.3 兩輪 62／63
（示範修好後模型算對的比例上升，但仍幾乎每份輸出都要修）——offset 修復不是可選優化，
是這個模型能用的前提。compile 段（gold plan → link → compile → engine gate）兩輪皆 55/55。
### Q-1 落地紀錄（plan-v1.4，2026-08-23）

移除 few-shot #1 兩處違反規則 4 的示範：a1 憑空的 `quantity`（evidence 切片 `拿取電動起子`
沒有任何數量）、a2 的 `destination.text` 由 `圖示位置`（**原文中不存在**）改為 `依圖示`
（落在 a2 自己的 evidence 窗 [7,16) 內）。後者不只是「改成字面」——它把一個**無從定位**的
角色變成**定位得到**的角色，正是 S-2 那套機制在別處要求的性質。

**量測（三份報告皆 v7、自述版本，收在 `eval-reports/local-14b/`）**

| 報告 | 成功 | acc | F1 | P | R |
|---|---|---|---|---|---|
| `plan-v1.3-rebaseline-*` | 48/55 | 0.7636 | 0.7069 | 0.7321 | 0.6833 |
| `plan-v1.4-run1-*` | 47/55 | 0.7273 | 0.7119 | 0.7241 | 0.7000 |
| `plan-v1.4-run2-*` | 47/55 | 0.7273 | 0.7119 | 0.7241 | 0.7000 |

**誠實的結論（複審更正過的敘述）**：

1. **`action_count_accuracy` 一致下降**約 0.036（少 1 案）。v1.3 三次（0.7636／0.7818／0.8000）
   與 v1.4 兩次（0.7273×2）**完全分離**——v1.3 最低仍高於 v1.4 最高。**每一組可得的比較都往下。**
2. **`boundary_span_f1` 測不出差異**：v1.4 的 0.7119 落在 v1.3 區間（0.7069–0.7350）內。
   P／R 同理。
3. **樣本量在任何結果下都不足以判定**：n=3 vs n=2 時「3 個全高於 2 個」已是最極端排序，
   rank test 單尾 p = 1/C(5,2) = **0.1**——**p<0.05 在這個設計下不可得**。所以「沒有統計顯著
   差異」是被設計保證的，不是關於世界的資訊。
4. **所以放行的依據是正確性，不是分數沒變。** 教材違反自己的規則就是缺陷，與分數怎麼動無關。
   代價（acc 少 1 案）已揭露於上表，未被埋掉。

⚠️ **先前把它敘述成「兩個指標方向互相矛盾」是錯的**（複審 P2-2）：實際是「一個指標乾淨分離、
另一個完全分不開」。寫成方向矛盾會讓一個一致的訊號讀起來像雜訊互抵。

**這批資料裡最值得留下的一行**：**兩次 v1.4 是真的獨立跑，卻逐案完全相同**——相隔 23 分鐘、
總 latency 不同、55 案的 `elapsed_ms` 全部不同，但忽略計時後有差異的案例是 **0**。
所以 v1.4 這一側接近決定性，`0.7273` 不是帶雜訊的抽樣、而接近真值；「兩邊都是雜訊所以測不出
差異」這個框架**對 v1.4 不適用**。（兩次相同不等於證明決定性，只是與「變異很小」相容。）
對照之下 v1.3 三次的失敗集合是洗牌的。

⚠️ **變異估計的已知弱點**（複審 P2-3）：上表的 v1.3 變異範圍（0.7636–0.8000）**用到了兩份
不自述版本的舊報告**——而我判定它們「不可採信、不能當門檻」的理由（無法證明是同一版 prompt）
同樣適用於「不能拿來估變異」。**在自述版本的資料裡，v1.3 其實只有 n=1。** 論證與自己的前提
不一致，記在這裡不掩蓋。

**守衛為何沒抓到**（記著，這是契約盲點）：`validate_planner_output` 只驗**帶 `text` 的
explicit 角色**，對 `explicit_unresolved` 完全不驗，對只帶 `value` 的 explicit 也只確認
「有值」、不問有無依據——兩處捏造都落在盲點裡。與 S-2 同源。


## 9. 已知限制與待決票（2026-08-22 checkpoint 明確不修的項目）

> 這一節的存在是為了讓「沒修」是**被決定的**、不是被遺忘的。每一項都附「為什麼這輪不修」。
> 資安項目**全部為既有缺陷**（本批未引入），但其中 S-2 被本批新增的 few-shot 放大。

### 資安（security-reviewer，2026-08-22）

| 票 | 級別 | 內容 | 為什麼這輪不修 |
|---|---|---|---|
| S-1 | High | `build_user_message` 對 `normalized_text` 與 `context` 無分隔符逃逸，使用者輸入可直接寫 `</wi_text>` 關閉資料區並附加指令；`normalize()` 的 NFKC＋lower **反而**把全形／大寫混淆變體還原成可用分隔符。實測逸出成功。影響邊界：`DDM_WI_AI_AUTO_ENABLED` 預設 false 且 `_eligible_auto` 另要求全部候選 `synonym_exact`，所以無法直接寫入 slot_inputs；實質風險是「被引導的草稿送去人工覆核」，在批次匯入（提交者≠覆核者）才是資料完整性問題 | 既有缺陷，修法（nonce 標籤或分隔符中和）動的是 prompt 組裝與 context 契約，屬獨立切片 |
| S-2 | High | 無憑據的角色值可通過 `validate_planner_output` 並直達 TMU：證據綁定只在 `status=="explicit"` **且** `role.text is not None` 才跑，且 `_role_covered_by_evidence` 是**雙向**子字串比對；而 `most_compiler` **完全不讀 `RoleValue.status`**（grep 為空），`_role_distance_cm` 直接吃 `role.value`／`role.unit` → distance_cm → TMU，**且 distance 這一路沒有任何 review 旗標**（對比 quantity 會掛 `quantity_policy_review` 並擋 auto）。三條實測可用的繞過：value-only 的 explicit、role.text 為 evidence 超集、`inferred`＋合法 `action_ref`。**本批新增的 few-shot #2 示範了 `{"status":"inferred","action_ref":"a1"}` 這個「無 text、不需證據」的角色形狀（對 object 無害且是拿分關鍵），等於教會模型該形狀可存在——套到 distance 上就是零憑據數字直達 TMU：這是風險放大，不是新漏洞** | 修法要動 `most_compiler`（觸發黃金值關卡與 ddm-validator 派工），且三種修法（compiler 拒收非 explicit 數值角色／`elif`→`if`／移除雙向比對的一半）各有取捨。**建議下一輪優先做「compiler 對非 explicit 的數值角色拒收或掛 review reason」**——成本最低且直接切斷唯一會產出錯誤工時標準的路徑 |
| S-3 | Medium | `NLDraftContextIn` 的 `station_hint`／`previous_row_summary`／`available_tools`／`available_locations` 無 `max_length`／`max_items`，而 `text` 有 2000 字元上限。三重後果：繞過長度上限的注入面、LLM 請求 input token 無上限（`max_tokens` 只管輸出）、`context_snapshot` JSONB 儲存放大 | 需與 `MAX_PARSE_TEXT_CHARS` 的權威計算一起定義，屬契約層改動 |
| S-4 | Medium ⭐**資安席建議往前拉** | `POST /api/v2/worksheets/nl-draft` 只有 `Depends(current_user)`，而姊妹端點 `POST /nl-drafts/{run_id}/reviews` 是 `require_role("analyst")`。`current_user` 對首次出現的員編會 JIT 建立 roles=[] 的 viewer → 任何通過 gateway 的員編可無限驅動地端 LLM 推論並寫入 `ai_parse_runs`。**這也是「模型名／prompt 版本對低權角色可見」的真正修法** | RBAC 改動需確認不打斷既有前端流程，屬獨立切片。⭐ **S-2 修好之後這條的份量變重**：S-2 的防護完全依賴覆核者看到旗標，而本條表示未授權的 viewer 可無限產生 run。修法是 1 行（`require_role("analyst")`）、無評測重跑成本，資安席評為單位工時風險削減最高的一項 |
| S-5 | Medium | `routing.ROUTING_REASONS` frozenset 定義後全 repo **零引用**；`compute_routing` 無條件 `reasons.extend(plan.unresolved)`，模型可控字串直接成為 `routing_reasons` 並持久化。方向保守（unresolved 非空必擋 auto），不構成權限提升，但宣告了卻不執行的白名單會誤導讀者 | 要嘛落地過濾、要嘛刪掉常數；屬清理，非阻擋 |
| S-6 | Low | 評測報告含本機絕對路徑 `gold_dir: /home/howard/...`（含 OS 使用者名）。**既有輸出行為**——上層正式報告 `wi-gold-latest.json` 同樣有 | 修 `wi_ai_eval.py` 輸出相對路徑即可，與 T-1 一起做 |
| S-7 | Low（＋**新增一條同源**） | `wi_ai_service.py:351-364` 的 except tuple 同時包住 LLM 呼叫、`_plan_from_planner_output` 與 baseline 比對三段，後兩段的真實程式錯誤會被歸類成「LLM planner failed」並降級到 rule parser。**不違反 No error bypass**（有 `logger.warning` ＋ `fallback_rule_based` 進 routing_reasons，是可觀測的宣告式降級），但錯誤歸因不正確。⚠️ **同一段的 **`:367`** 是 `logger.warning("LLM planner failed; falling back to rule_based: %s", exc)`——會把含 `user:pass@host` 的 httpx 401 訊息原文寫進應用日誌**（2026-08-22 修 `planner_eval` 的憑證洩漏時發現；報告端已用 `_redact_urls` 遮掉，log 端沒有）。風險等級與報告端不同（日誌不入版控），但同一個字串、同一段程式 | 拆 try 區塊即可，非阻擋。**日誌那條併進本票一起做**——動那幾行時本來就會碰到；掃過 `src/` 其餘路徑：DB 與 API 回應都不寫例外原文，只有這一處 log |
| S-8 | **High（證據判準未關閉；非有限值已關閉）** | **`x_seconds` 是 S-2 同一種病的第三條路**：`compile.py` 的 CM 分支讀 `process_kind.value`（unit 為 s/sec/秒）→ `x_seconds` → `most_engine/calculate.py` 的 `seconds_to_tmu(seconds) * rep`。**兩半要分清楚**：⑴ **非有限值**（`nan` 秒數會算出 `total_tmu: nan`＝送給前端的不是合規 JSON；`inf` 丟 `InvalidOperation` → HTTP 500）——**2026-08-22 已修**，加了有限值關卡並掛旗標；⑵ **證據判準**——`process_kind.value` 的秒數**完全不經過** `numeric_claim_is_evidenced`，無 status、無證據檢查、無 review 旗標，模型捏造的秒數直達 TMU。**這一半仍開著**。D3-025 的 `x_seconds_required` 只處理「**沒有**秒數來源」，對「捏造」無感 | ⑵ 未做的理由：`nlp/quantities.py` 沒有秒數抽取器，讓 `numeric_claim_is_evidenced` 支援 `kind="seconds"` 是真工作量不是一行。**已在 spec §10.3 與 `policies.py` 註解明寫為已知未關閉**，避免下一個讀者以為整類都有守門了。建議與 **S-11** 一起做（同一族：數值進 TMU 前缺檢查）|
| S-9 | Low | **旗標不跟著資料走**：`distance_unevidenced_review` 等活在 `CycleDraft.issues` 與 `routing_reasons`，IE 一旦接受草稿寫進 `most_cycles.slot_inputs`，沒有任何欄位記下「這個 cycle 曾含無憑據數字」。保護完全等於「覆核者當下有看到」 | 與設計意圖一致（auto 已擋、必須有人看），但事後無法稽核哪些已接受的 cycle 當初帶過旗標。要補得動 schema |
| S-10 | Low | **routing reason 沒有中文文案**：前端 `AiDraftPanel.tsx:182-185` 原樣 `.slice(0,3).join()`、`ActionCard.tsx:56-58` 原樣 join 進 i18n 樣板（樣板翻譯、key 不翻），`shared/i18n/` 裡 per-key 文案零筆。覆核者看到的字面是 `distance_unevidenced_review`。spec 先前宣稱「前端依這些 key 顯示中文文案」為不實敘述，已更正 | ADR-032 已交付的前提下是真實但小的缺口；屬 i18n 獨立切片 |

| S-11 | **Low–Medium（非 LLM 入口；持久化被 DB 附帶擋住）** | **`POST /api/v2/minimost/calculate` 吃得下 NaN**：`schemas/v2/most.py:27` 的 `reach_cm: float = 0` 無有限值約束；`CycleIn.model_validate` 接受 → `compute_cycle` 回 **A0 index 24／total 30.0**。傳輸層實測**真的收**（TestClient 送裸 `NaN` 字面量 → 200，Starlette 走 `json.loads`）；瀏覽器路徑打不到（`JSON.stringify` 對 NaN 產生 `null` → 422）。**但污染不了資料**（資安席驗到底）：`MostCycle.slot_inputs`／`computed` 是 **JSONB**，PostgreSQL 的 jsonb **拒絕** NaN／Infinity，而 `slot_inputs` 與 `total_tmu` 在同一個 INSERT → 整筆 rollback。所以最壞後果是「呼叫者自己拿到一個錯的 30 TMU 回應」，**不會寫進 `most_cycles`、不會流向 LineBalance** | **延後（三席一致同意）**。⚠️ 但兩件事要記住：⑴ 失敗模式是**未處理的 DB 例外 → 500**，不是乾淨的 422；⑵ **這道保護是附帶的**——來自 JSONB 的型別語意而非刻意的守衛，而 `total_tmu` 是 `Numeric` **吃得下 NaN**。哪天 `slot_inputs` 改存法、或出現只寫 `total_tmu` 的路徑，保護就沒了。治本是在 `CycleIn` 加 `allow_inf_nan=False`（Pydantic 一個 model_config），但 `schemas/v2/most.py` 是 CLAUDE.md 明列的**黃金值關卡觸發檔**＋對外 API 契約，不與 compiler 邊界綁在同一次驗證裡（真出事會分不清哪一邊）。建議與 **S-8** 一起做 |


### 測試與評測基礎設施

| 票 | 內容 | 為什麼這輪不修 |
|---|---|---|
| T-1 ✅**已完成 2026-08-22**（`wi-gold-report-v7`：頂層 `planner_run` 記 planner/model/prompt_version/llm_timeout_s；刻意不記 base_url／api_key 並有反向斷言釘死；`gold_dir` 改相對路徑＝順帶關閉 S-6）| **評測報告不記 model 也不記 prompt version**（七份全部 `grep -c qwen` = 0，無 `prompt_version` 欄，`planner` 只寫 `"llm"`）——「哪份報告＝哪一版」完全靠**人工命名的檔名**（腳本產的是 `wi-gold-<stamp>.json`，這批是事後手改名）。**這與本批修掉的 `provenance.parser` 是同一種病**：一個關於來源的主張，而該檔案自己無法佐證。`wi_ai_eval.py` 加兩行即可（順帶修 S-6 的絕對路徑，並在 README 的重跑指令補「然後改名」這一步） | 動的是評測腳本，與本批的 prompt／provenance 改動無關，獨立一票比較乾淨 |
| T-2 | **few-shot #1 的 `a1.quantity = {"value":1,"status":"explicit","unit":"count"}` 是憑空捏造**——原文 `拿取電動起子` 沒有數量，正是新規則 4「禁止猜測／不得用佔位字」所禁止的；`a2.destination.text="圖示位置"` 亦為改寫（原文是 `依圖示`）。守衛抓不到，因為 `validate_planner_output` 只驗**帶 `text`** 的角色、對 `explicit_unresolved` 完全不驗。考慮到報告裡 `inferred_without_ref:aN:quantity`（g34）與 `action_ref_unknown:a1:distance`（g49），數值型角色正是模型最會出錯之處，這不是純理論 nit | **使用者裁決（2026-08-22）留到下一輪**：改教材必須重跑 55 案評測（約 25 分鐘）才知道有沒有掉分，不宜與本批混在一起 |
| T-3 | 切分守衛的**檢查 (2) 在 gold 上是空跑**：gold 零個 `acquire→move_place` 相鄰對，且 gold plan 是 rule parser 預標註（60 個 action 只有 3 個有 roles、全集 1 條 dependency、23 個 move_place 全部沒有 explicit object）。**連帶：`_link_between` 這支 helper 從未對真實 gold 資料執行過。** 前瞻脆弱性：gold 若改成從已核准的 LLM plan 回填，`(2b)` 會對**合法**的 acquire→move_place 相鄰誤報 | 判準對 few-shot 是對的（有 `test_guard_catches_the_plan_v11_mis_segmented_shot` 直接證明機制會動），缺的只是 gold 背書。已在 spec §7.2 與測試 docstring 誠實標註。**動 gold 回填流程之前必須先處理** |
| T-4 | `_KNOWN_GOLD_TEXT_OVERLAP` 只比對**精確全等**：gold 原文多一個字的 near-copy 可全身而退，而就洩題而言效果幾乎相同 | 需要相似度判準（非精確比對），設計問題非一行可修 |
| T-5 | 慣例動詞表的下界斷言是 `>=`：只擋詞表流失（拿掉「抓握」會紅），**不擋 gold 新增使用表外詞彙的案例**（checked 仍 10 → 綠，該案靜默不受檢）。few-shot 側零下界（實測 checked 數 shot#1=0、#2=1、#3=0、#4=0——檢查 (1) 只覆蓋 4 則中的 1 則，**英文示範完全沒有切分覆蓋**，詞表純中文） | docstring 已誠實標註；補英文詞表與 few-shot 側下界屬獨立改善 |
| T-6 | settings 洩漏偵測器只涵蓋**同檔前面的測試**（docstring 有誠實寫）。通則解是 `tests/conftest.py` 放 autouse fixture 每個測試後 `cache_clear()` | 全域 fixture 影響面大，需單獨評估 |
| T-7 | **spec §7.2 的 prompt 區塊與 `plan_v1.SYSTEM_PROMPT` byte-identical 這個性質沒有任何測試在守**——目前靠人工比對（本次由 code-reviewer 手動確認）。同理 `test_system_prompt_enumerates_every_dependency_type` 驗的是 `SYSTEM_PROMPT` 常數，**不是模型實際收到的組裝訊息**（常數＋schema＋few-shots） | 兩者都是「加一支測試」等級，但需決定文件與常數誰是權威 |
| T-8 | **gold 語料承載不了任何 roles-based 判準（結構性限制，同一模式已出現兩次）**：gold plan 是 rule parser 預標註，60 個 action 只有 3 個有 roles、全集 1 條 dependency，**且 55 案的 `expected_cycles` 距離值全是 `0.0`**（整個 gold 只有 `g02` 一個 quantity 數值角色）。後果：(a) 切分守衛的檢查 (2) 在 gold 上空跑（見 T-3）；(b) **「gold 55 案零新旗標」不能當 S-2 距離判準的證據**——那同樣是空轉，實質結論來自 validator 另建的 20 個探針。⚠️ 這不是某一支測試的問題，是**語料本身的表達力上限**：任何依賴 roles／dependency／數值角色的守衛，都無法用現有 gold 取得背書 | 要解得補 gold 的 roles（IE 親筆或從已核准 LLM plan 回填），而回填又會觸發 T-3 的前瞻誤報。**動 gold 回填流程時必須把 T-3 與本條一起考慮** |
| T-9 | **`AiDraftPanel.tsx:184` 只顯示 `routing_reasons.slice(0, 3)`**（其餘在 `title` tooltip）。一張同時帶 `next_operation`／`quantity_policy_review`／`missing_core_*` 的 draft 會把 S-2 的新旗標擠出可見文字之外。旗標仍在 payload、仍擋 auto，但 spec 與程式註解說的「覆核者一定看得到」比 UI 實際做到的強 | 與 S-10（reason 無中文文案）同屬覆核 UI 的可見性，建議一起做 |

| T-10 | **報告自述的三個已知形式弱點**（T-1 交付時記票，皆需動與 `wi_ai_service` 共用的契約或加欄位）：⑴ **`model_served_confirmed` 缺席**——`llm_client.py:94` 是 `data.get("model") or self._model`，伺服器沒回 `model` 欄時 served **退回請求值**，此時 served==requested 但並非真的被證實；這個 caveat 目前只活在報告 `note` 的散文裡，**沒有任何東西能對它做斷言**。修法是 `LLMRawResponse` 多帶一個布林。⑵ **`model_served_variants` 丟掉次數**——「55 案只有 1 案成功」與「55 案全成功」在 `planner_run` 裡長得一模一樣（可從 `planner_failures.count` 還原，因 `observe_served` 只在成功返回後呼叫，但要多繞一步）；改成 `{model: count}` 順手解決並看得到比例。⑶ **`model_requested` 未過 sanitizer**——來自本機 `DDM_LLM_MODEL`（操作者自設，威脅模型不同），但同一行 stdout 也會印它；要硬化就是把 `_sanitize_model_name()` 套到建構子上、一行 | ⑴⑵ 動共用契約／欄位形狀，不與 T-1 綁在同一次驗證；⑶ 威脅模型不同（本機設定 vs 遠端回應），非必要 |
| T-11 | **`llm_client.py:94` 的源頭截斷**：`model` 目前只做 `str(model)`，長度無界（資安席實測一個回應可塞 100KB）。T-1 在 `wi_ai_eval` 端加了截斷，但**DB／API 路徑（`ai_parse_runs.llm_raw_response` → `provenance.model`）仍是無界的** | 動到與 `wi_ai_service` 共用的契約。優先度 Low——觸發前提是 endpoint 已被控制，那時報告本來就沒有證據力 |


### 設計取捨（已決定，記錄理由）

| 票 | 內容 |
|---|---|
| D2-A | **`input_hash` 不含 `PROMPT_VERSION`**（spec §10.2）。維持現狀的理由：P1-2 的本質是「謊報」，那個已經消失——舊列回誠實的 `null`、新列回真版本；剩下的只是「陳舊」，而陳舊現在可觀測。改 hash 會讓既有快取**全數失效**並動到 §10.2 定義，屬 D2。⚠️ **落地觸發條件**：下次 prompt 有實質變更且要出版之前。⚠️ **絆線**：`test_cached_replay_reports_prompt_version_of_the_run_not_current_constant` 的 `assert cached is True` 等於把這個取捨釘成斷言——D2 落地時它會紅，**那個紅是預期的，不是迴歸**。⚠️ 另注意「可觀測」目前只到 API 層：前端不顯示 `prompt_version` |
| D2-B | `slot_linker:{planner}` 前綴在 `used_fallback and rule_candidates and not linked` 那一支上**過度宣稱**（該批 candidates 出自 rule adapter 而非 `SlotLinker`；run 上沒有可區分兩者的訊號）。`planner` 成分無條件為真，呼叫端註解已誠實記載。要無條件精確可改 `run_slot_candidates:{planner}` |
| D2-C | `test_extra_is_merged_last` 把「`extra` 覆蓋得掉 `parser`／`fallback`」釘成斷言（docstring 說明是**記錄既有行為**而非鼓勵）。未來若要硬化（禁止 `extra` 蓋掉受管欄位），需同時改這條測試 |
| D2-D | **retention 會抹掉來源資訊**：`llm_raw_response` 被清成 NULL 後 `prompt_version` 與 `model` 都回 `None`（誠實但不可回溯）；更嚴重的是 `planner = "rule_based_v1" if run.fallback or not llm else "llm"` 會讓 `fallback=False` 的 LLM run 重播成 `rule_based_v1`——**與本批修掉的謊報同類，觸發源是 retention**。耐久的權威訊號是 `fallback` 欄。（`scripts/prune_ai_runs.py` 目前不存在，實作前必須先處理這條）|

## 10. 下一輪工作佇列（2026-08-22 排定）

> 排這張表的目的是讓「下一輪要做什麼」不必再從報告裡重推一次。每項都寫明**進場條件**
> （做之前必須先成立的事）與**完成證據**（做完要拿什麼出來證明）。
> 優先序由使用者裁定：S-2 先修（進行中），其餘依序。

**建議執行順序（2026-08-22 收線時的判斷）**：

1. **T-1 → Q-1**：先讓 `wi_ai_eval.py` 把 `DDM_LLM_MODEL` 與 `plan_v1.PROMPT_VERSION` 寫進報告（兩行），**再**動 few-shot。否則 Q-1 的評測報告版本歸屬又只能靠人工檔名——那正是本輪 commit 裡批評過的病（一個關於來源的主張，而檔案自己無法佐證）。
2. **S-8 ＋ S-11 一起做**：同一族（數值進 TMU 前缺有限值／證據檢查），且兩者都會觸發黃金值關卡，分兩次做等於驗兩次。
3. **S-4**：一行 `require_role("analyst")`、無評測成本，資安席**兩次**建議往前拉——理由是 S-2 的整套防護最終依賴「覆核者看得到旗標」，而 S-4 表示未授權的 viewer 就能無限產生 run。

### Q-1 · few-shot #1 的 `a1.quantity` 憑空 explicit（＝§9 的 T-2 / 複審 P2-7）

| 項目 | 內容 |
|---|---|
| 問題 | `prompts/plan_v1.py` few-shot #1 的 `a1.quantity = {"value":1,"status":"explicit","unit":"count"}`，而該 action 的切片是 `拿取電動起子`——原文沒有任何數量。這正是 v1.3 規則 4 明文禁止的「禁止猜測／不得用佔位字」。同則的 `a2.destination.text="圖示位置"` 亦為改寫（原文 `依圖示`）|
| 為什麼守衛抓不到 | `validate_planner_output` 只驗**帶 `text`** 的角色，對 `explicit_unresolved` 完全不驗——這是與 §9 S-2 同源的契約盲點 |
| 為什麼重要（不是理論 nit）| 評測報告顯示數值型角色正是模型最會出錯之處：`inferred_without_ref:aN:quantity`（g34）、`action_ref_unknown:a1:distance`（g49）。教材在示範「數值可以沒有出處」 |
| 進場條件 | 無（可隨時做）。但**必須與評測重跑綁在同一次**，不可只改教材不量 |
| 完成證據 | 拿掉該角色後**重跑 55 案評測**（`--planner llm`，qwen2.5:14b，約 25 分鐘），與 `plan-v1.3-run1/run2` 對照：成功數與 F1 落在 48–50 / 0.733–0.735 的變異帶內即為無退步。**升 `PROMPT_VERSION`**（本檔硬規定：內容有動就升版）|
| 風險 | 改教材可能連帶影響其他案例的切分——單輪數字不足以判斷，必要時跑兩輪 |

### Q-2 · 持續失敗 4 案（`g13`／`g46`／`g47`／`g48`）

兩輪交叉比對確認為持續失敗（非抖動）。**三種不同性質，不要當成一個問題處理**：

| 案例 | 敗因 | 性質與下一步 |
|---|---|---|
| `g46`／`g47` | `action_ref_unknown:a1:object`（`g46` run2 為 a1＋a2 各一次）| **規則寫了但模型不遵守**：v1.3 規則 4 已明文「第一個 action 前面沒有任何 action 可指，它的角色不得是 inferred」。加規則已達邊際效益遞減。⚠️ 注意 schema 層拒絕**已經在做**（`llm_client` 附了 JSON Schema），所以下一步不是「改成 schema 層」——而是**帶著具體錯誤訊息 retry**，或接受它作為 fallback 案例 |
| `g13` | run1＝自創 `same_hand`（`json_or_schema`）／run2＝`explicit_without_evidence:a1:hand` | **同一案兩輪敗因不同**，代表它在多個判準邊緣。`same_hand` 那次證明「白話列出 enum」沒有蓋過 schema 已提供的 enum。先觀察，不要為單一案例加規則 |
| `g48` | `quantity.value` 收到字串「多顆」→ Pydantic float 解析失敗 | **不是 prompt 可解**。屬 §7 待決 #1（quantity 展開 policy），需 IE 裁決「多顆／數個」這類非數值量詞如何表示。裁決前不要試圖用 prompt 繞過 |

| 項目 | 內容 |
|---|---|
| 進場條件 | `g48` 需 IE 對 §7 待決 #1 裁決後才動；`g46`／`g47`／`g13` 無前置條件 |
| 完成證據 | 任何改動都要**兩輪**評測對照（單輪落在 ±2 案的變異帶內，不足以宣告修好）。抖動尾巴 `g24`／`g34`／`g49`／`g35` 同理——**不要用單輪結果判斷這幾案是否修好** |

### Q-3 · 承接自 §9 的技術債（依成本排序，皆為獨立小切片）

1. ~~**T-1**：`wi_ai_eval.py` 把 `DDM_LLM_MODEL` 與 `plan_v1.PROMPT_VERSION` 寫進報告，順帶修 S-6 的絕對路徑。~~ ✅ **2026-08-22 完成**（`planner_run` ＋ schema v7）。⚠️ 但**既有的十四份報告都是 v7 之前跑的、內容不自述版本**，那些的版本歸屬仍只有檔名可依——Q-1 的對照基準要記得這件事。**做完 T-1 之後，Q-1 的評測報告才有自我佐證的版本歸屬**，不必再靠人工檔名——建議排在 Q-1 之前。
2. **S-7**：拆開 `wi_ai_service.py:351-364` 過寬的 except tuple，讓自家 bug 不再被歸類成「LLM planner failed」。
3. **S-5**：`ROUTING_REASONS` 白名單落地或刪除（宣告了卻不執行比沒有更糟）。
4. **T-6**：`tests/conftest.py` 加 autouse fixture 每測試後 `get_settings.cache_clear()`（通則解，取代目前只涵蓋同檔的偵測器）。
5. **T-7**：把「spec §7.2 的 prompt 區塊 ＝ `SYSTEM_PROMPT` 常數」變成測試（目前靠人工比對）。
